# 项目规范与约束

本文件是 Coding Agent 与开发者共同使用的持久项目上下文。修改项目之前，必须先阅读本文件以及 `PLAN.md` 中与当前任务相关的部分。本文件应保持简洁，并在项目级决策、当前状态或已知问题发生变化时及时更新。

## 目录

- [项目目标](#项目目标)
- [架构原则](#架构原则)
- [技术架构（已确认）](#技术架构已确认)
- [技术栈](#技术栈)
- [核心功能](#核心功能)
- [编码约定](#编码约定)
- [单人开发与 Git 约定](#单人开发与-git-约定)
- [远程仓库（GitHub）](#远程仓库github)
- [Agent 工作规则](#agent-工作规则)
- [当前状态](#当前状态)
- [设计决策（要点，按时间倒序）](#设计决策要点按时间倒序)
- [路线图](#路线图)
- [近期记录（摘要）](#近期记录摘要)
- [已知问题](#已知问题)


## 项目目标

开发一款 Windows 桌面的、接入大模型的志书撰写工具，帮助地方党史方志办公室的公务员自动收集、整理、归纳、撰写、审校志书，覆盖"资料收集 → 志稿撰写 → 初稿完成"的完整业务闭环。

## 架构原则

- **本地优先**：所有用户个人数据均保留在本地。除（1）调用大模型时必要的交互、（2）访问用户提供的信源网址外，其他操作均在本地进行。初版不要求账号，不建自有云服务。
- **资料闭环（信源白名单）**：资料只能来自两类来源——用户手动导入的文件、用户输入的信源网址。工具**绝对不得从其他渠道自行获取信息**，撰写时只基于这两类来源。
- **人机协同**：AI 负责初稿生成、资料整理归纳、来源标注；史实考证、矛盾裁定、事件补充、终审定稿由人工主导（遵循"依靠不依赖、赋能不替代"原则）。
- **全程可溯源**：AI 生成的志稿每个片段在用户需要时都能展示其原文来源（哪个信源/哪篇文章），供用户审核。
- **分层隔离**：操作系统能力（文件读写、信源抓取、数据库、模型调用）与界面层分离，仅通过规模小、类型明确且经过参数验证的接口向界面层暴露能力，避免界面层获得不受限制的本地能力。

## 技术架构（已确认）

**方案：Electron + React + TypeScript**。Main 进程（本地文件、SQLite、信源抓取、LLM 调用）/ preload 安全桥 / React Renderer 三层。Renderer 使用 `sandbox: true` + `contextIsolation: true`，不暴露 Node 能力。

### 关键架构决策点（现状）

| 决策点 | 方案 | 状态 |
|---|---|---|
| 桌面框架 | Electron 43 + React 18 + TypeScript strict（electron-vite 构建） | 已确认 |
| 本地数据库 | SQLite（better-sqlite3 13，WAL + 外键 + 嵌入式迁移框架 Migration 001–039） | 已确认 |
| 检索增强（RAG） | 本地向量嵌入（BGE-small-zh-v1.5，transformers.js + onnxruntime-web WASM 后端，纯本地）+ 词法 bigram 过滤式检索 + 可选 LLM 摘要粗筛；模型/引擎不可用自动降级纯词法 | 已确认 |
| 文档解析 | PDF(pdf-parse) / Word(mammoth + word-extractor) / WPS(签名分发) / Excel(xlsx 0.20.3) / TXT / MD / 图片OCR(tesseract.js) | 已确认 |
| 工作区资料库 | 指定本地文件夹即资料库：sha256+mtime/size 指纹对账、chokidar 实时监听 + 聚焦/进资料库/每分钟确定性兜底、双向同步（删除→回收站、改名→重命名文件） | 已确认 |
| LLM 接入 | OpenAI-compatible Provider（兼容 DeepSeek、智谱等），safeStorage(DPAPI) 加密存密钥；任务可固定 Provider，未固定回退全局当前 Provider | 已确认 |
| 写作规范 | 规范文档库（`style_guides`，Phase 6.4.1）：默认规范（合并「志书文体文风」+「志书行文规则」）自动注入生成；可多篇、可设默认、可另存/覆盖/重命名/删除；第二步可选任务级参考范本（Phase 6.4.2） | 已确认 |
| 编辑器 | TipTap 2.27 + tiptap-markdown，初稿为单编辑器连续 Markdown 文档，800ms 防抖整稿保存 | 已确认 |
| 打包发布 | electron-builder（Windows NSIS，GitHub Actions tag 触发） | 已确认 |

## 技术栈

- 运行时 Node.js LTS / npm；桌面框架 Electron 43；构建 electron-vite + Vite；Renderer React 18 + TS strict；测试 Vitest（内联单测）
- 本地数据库 better-sqlite3；RAG：@huggingface/transformers + onnxruntime-web（WASM，经 `vendor/onnxruntime-node-stub` 转发）
- 文档解析：pdf-parse / mammoth / word-extractor / jszip / xlsx(SheetJS 0.20.3) / tesseract.js；PDF 查看 pdfjs-dist（主线程 LoopbackPort worker）
- 编辑器：@tiptap/* + tiptap-markdown；打包：electron-builder（Windows NSIS）
- 目标平台：Windows

## 核心功能

### 1. 收集材料

- 资料来源：**工作区文件夹内文件**（指定文件夹即资料库，多级子目录自动同步入库）、**用户输入的信源网址**、**网页资料库**（注册站点后生成初稿时自动检索相关文章）。
- 资料可打自定义标签（独立 `source_tags` 关联），撰写时按任务范围（当前固定为全部长期资料）检索使用。
- **写作规范 skills**：预设 + 自建的志书写作规范（通用规范/部类细则），生成初稿时注入提示词。

### 2. 撰写（聊天式工作台）

- 任务创建即建（标题默认"新建任务"，右键重命名）；用户输入撰写要求 → 自动整理摘要 → 网页资料检索 → RAG 检索 → 矛盾预扫描 → 大模型生成整篇连贯 Markdown 初稿（第 0 稿）→ 矛盾定位审查回填。
- 生成后输入框变为自由对话（注入当前初稿 ≤12000 字上下文）；对话历史持久化（`task_messages`）。
- 生成过程推送阶段进度 + 百分比 + 预计剩余时间；失败（缺标题等）由大模型返回详细报错。

### 3. 初稿人工审核（并入 Phase 3 实现）

- **出现矛盾**：正文插入 `【矛盾#N】` 标注，点击弹出多说法对比（含来源文件链接），支持采纳（正文本地替换 + 撤销兼容）与忽略；不在正文的矛盾归入「警告」清单。
- **文段来源询问**：框选正文右键询问来源——本地精确匹配 → 生成上下文溯源 → 过滤式检索 → LLM 兜底，回复中 `#N` 渲染为可点击来源链接。
- **修改文段**：直接在编辑器中编辑，自动保存。

> **2026-08-11 决策**：版本迭代与管控已从产品范围删除，每个任务仅保留一稿（初稿）；"事件缺失补充"暂不在当前范围。

## 编码约定

- 采用 TypeScript strict，除非有明确记录的理由，否则不得引入 `any`。
- 代码标识符、文件名、API 名称、数据库字段和 Git Commit 标题使用英文。
- 面向用户的文字不得硬编码在组件里，应集中在文案资源（`src/renderer/src/i18n/zh-CN.ts`）。
- 界面组件只负责呈现与交互；持久化、网络请求、解析、检索和 AI 逻辑放入服务层（主进程）。
- 不得向界面层提供不受限制的 Node.js、文件系统、Shell 或数据库访问能力；每个接口请求都必须在主进程中验证并返回结构化结果（`ApiResult<T>`）。
- 外部资料（导入文件、信源内容）均视为不可信输入，显示前必须进行安全清洗。
- 不得在源码或普通日志中写入 API Key、凭证、个人路径或资料正文（诊断日志统一脱敏）。
- 本地数据库结构确定后，所有 Schema 变更必须通过迁移完成。
- 为文档解析、信源抓取、数据库操作、AI 响应处理、初稿生成与矛盾检测编写针对性测试。
- 面向用户的流程必须处理加载、空数据、成功、部分失败和错误状态。
- 避免无关重构；修改共享类型或接口协议时，必须同时更新所有调用方与相关文档，不得静默修改。
- 为任务创建的临时脚本、调试文件、预览产物等，在任务完成后必须及时删除，不留工作痕迹；确需保留的演示产物须明确归档说明。

## 单人开发与 Git 约定

- 本项目为**单人开发**，`PLAN.md` 中的模块划分是代码组织边界，不涉及多人协作。
- 使用自己的 Git 身份提交代码；每个 Commit 只处理一个明确目的，并包含其验证结果（测试/检查通过情况），便于回溯。

## 远程仓库（GitHub）

- **仓库地址**：`https://github.com/RegeonChen/XieZhiShu`，是开发版本控制的权威远程源。默认分支 `main`，单人开发默认直接提交；大改动可开 `feature/*` 分支。
- **同步规则**：开发前先 `git pull --rebase`；每完成一个任务并验证后提交并 `git push`。
- **版本发布规则**：语义化版本（SemVer），里程碑打 tag `vX.Y.Z` 并在 GitHub Release 记录发布说明。**每个 `vX.Y.Z` 发布必须打包并上传 Windows NSIS 安装包作为 Release 资产**：`npm run dist:win`（electron-builder），产物 `dist/XieZhiShu-Setup-${version}-x64.exe` 与对应 `.blockmap`，供用户下载安装。本机无需 Visual Studio：better-sqlite3 使用 N-API 预编译产物，故 electron-builder 配置 `npmRebuild: false` 跳过源码重编译。
- **禁止事项**：不得对 `main` 使用 `git push --force`、`git reset --hard`、`git checkout .` 等破坏性命令；未经用户明确要求，不执行 commit、push、tag、release 等远程相关操作。
- **敏感与数据隔离**：API Key、`.env`、本地数据库文件、用户资料样本不入库；`.gitignore` 排除 `node_modules`、`dist`、`release`、`out`、模型文件等；项目文档（本文件、`PLAN.md`、`init.md`、`docs/`）纳入版本管理。

## Agent 工作规则

- 修改代码前，先阅读本文件与 `PLAN.md` 中当前执行的任务。
- 先确认仓库的真实状态，不得假设文件或依赖已经存在。
- 当某项选择会改变产品范围、共享协议、安全性、存储数据或平台行为时，将决策及理由记录到本文件"设计决策"或"近期记录"。
- 报告任务完成前，必须运行当前环境中最相关的检查（typecheck / 单测 / 构建）。
- 未证明任务的 `Verification` 验收条件之前，不得将任务或阶段标记为完成。
- 当修改对项目产生实质影响时，更新"当前状态"和"已知问题"。

## 当前状态

截至 2026-09-10（Phase 7.1–7.5「生成汇编」重构：连续文档 + 版本差异管控 + 悬浮对话框人机协同编辑）：

- **已完成**：Phase 1（脚手架/共享契约/数据库迁移框架）、Phase 2（文件导入/信源抓取/标签）、Phase 2.1（删除与标签重构）、Phase 2.2（工作区资料库 + 实时双向同步 + 自动同步触发源）、Phase 3.1（LLM Provider 配置）、Phase 3.2（BGE 向量嵌入 + 词法/向量混合检索 + LLM 摘要索引）、Phase 3.3（范本 → 后重构为写作规范 skills）、Phase 3.4（连续整稿显示 / 摘要粗筛 / 检索过滤式 / 重新生成）、Phase 3.5（聊天式工作台 + 对话持久化 + 进度提示）、Phase 3.6（预设大模型 + 获取 API key 指引）、Phase 3.7（矛盾预扫描 → 生成注入 → 定位审查三次调用链路、编辑器内嵌矛盾标注与弹窗、采纳本地修订 + 撤销兼容、矛盾/警告分类、文段来源询问、来源文件打开）、网页资料库（站点注册/发现/粗筛/增量抓取，任务绑定缓存文章）。
- **产品范围**：收敛为"资料收集 → 撰写 → 初稿完成"；版本管理已删除（数据库保留旧列不动），每个任务仅保留初稿。
- **验证基线（截至 2026-09-12，Phase 7.7 收尾 + 网页第一/二/三批 + 抓取上限复议 + 索引可诊断与提速之后）**：typecheck 零错误；vitest 单测 **264 项通过**（总计 265 项，另有 1 项 watcher chokidar `unlink` 在本机不触发，为既有环境问题，非功能回归）；生产构建成功。端到端实测（真实大模型生成/整合提取效果/矛盾取舍/对话编辑/导出与第三步/站点抓取）部分场景仍待用户操作。
- **Phase 7「生成汇编」重构（进行中）**：7.1 数据模型（Migration 030/031/033）、7.2 管线（细读筛选 → 整合提取 → 矛盾扫描）、7.3 连续文档查看器 + 来源圆标、7.4 版本差异、7.5 悬浮对话框人机协同编辑（**软件内改动汇编的唯一入口**；对话修改后自动进入复核态「采纳 / 回退」）**均已实现并经用户三轮验收整改**；**7.6 导出/第三步/回收站/来源删除 + 左栏下线（2026-09-10 用户裁定把原 7.10 并入本阶段；生成入口裁定 D7=A：悬浮面板兼作"生成模式"）** 已完成并推送。**7.7 收尾清理进行中**：切片 A–D 已提交——删 `compilation-adjust.ts`、`purify-service.ts`、`repair-service.ts` 与两个死阶段函数（A/B1）；「大模型修正」链路端到端下线（渲染层徽标/弹窗、`Compilation.repairs`、两个 IPC 通道、`compilation-repairs.ts` 仓储、`CompilationItemInput.repair`，B2）；**Migration 036 破坏性清理**（`DROP TABLE compilation_repairs`/`compilation_card_recycle_bin`，回收站收缩为仅矛盾，撤销快照去掉 `repairs`，C）；删除四个无界面入口的通道 `compilation:updateItem`/`deleteItem`/`version:diff`/`version:restore`（D）。切片 E（死样式/文档/演示数据与基线）本轮完成；切片 F（真实 Provider 端到端与性能基线）待用户操作。**⚠ `compilation-undo.ts` 内存快照栈按用户要求保留**（原 7.7 计划里"删除快照栈"一条已作废）。详见 `PLAN.md` 7.6 / 7.6.1 / 7.7。
- **进行中**：三段式撰写重构 Phase 6.0–6.6 已实现（数据模型/汇编生成/三步向导/生成链路/删 skills+默认规范/规范库/范本/三套主题/编辑器深改；Migration 016–020）。Phase 6.8「按步骤分别指定大模型」已完成（设置页全局第 1/3 步默认模型，大模型配置一律以步骤默认模型为准，已删全局默认+任务级下拉，重新生成修复走第 3 步模型）。**Phase 6.4.4 资料卡片「提纯」已完成（2026-09-08 新增、2026-09-10 实测后收紧）**：管线改为 `AI 分窗细读 → 提纯 → 修正 → 卡片矛盾扫描`——针对「整段成卡」导致卡片夹带大量无关内容（实测 191 张卡 / 48,063 字中相关句仅约 31%），由大模型对整段卡片做**逐字摘录**（严格子串 + 本地校验，置于修正之前以保持修正「标记 + 回退」与卡片一对一）；新增 `src/main/writing/purify-service.ts`、`runPurifyPhase`、`purifyScan` 结果字段与生成汇总展示。2026-09-10 真实数据复议后三项改造：**判定口径改为「写通测试」**（该段文字会不会写进题为《主题》的志稿正文——同为教育大领域但对象/学段/业务不是本主题的一律删除）、**本地句读吸附**（起点左扩到句读边界、终点右扩到句末标点，只外向不内向，修掉实测 43 张起点不在句读边界 + 11 张结尾半句的残缺卡片）、**性能与可观测**（批次 50 张 / 18000 字、预算 900s、**按 Provider 并发并行**、去掉无用的 `reason` 字段、漏答重问 + 解析失败重试、逐批诊断日志与 `purifyScan.passthroughCards`）。**Phase 6.4.3 资料卡片二次加工已完成（2026-09-08 已按用户需求改版为「大模型修正」）**：原实现为 Step-1 生成汇编后追加 LLM 语义补全扫描（`compilation_repairs`，Migration 021），绿框前后对比 + 采纳/不用，可进回收站恢复；现改为——修正**提前到生成管线内、卡片矛盾扫描之前**（先修好卡片再查矛盾）、**默认全部应用**、卡片上以「✎ 经过大模型修正」标记承载（点开弹窗看修正前原文与理由，可回退/再次应用）、**不再进入回收站**（回收站降为「资料卡片 / 矛盾」两类；`compilation_repair_recycle_bin` 随 Migration 029 删除）。时间戳自动补齐不算修正（静默、无标记、不可回退）。**2026-09-10 修正提示词收紧**：时间补齐范围扩为「无时间戳**或时间缺少年份**」（实测 12 张卡只有月日，如 `5 月 19 日`、`7—9 日`），要求时间标注**必须含年份**且不得编造，本地 `hasYear`/`shouldFillTs` 只采纳含 4 位年份的结果；残缺判定补充「句子起点/终点不完整、缺少主谓宾、指代不明」，并明确**优先补全而非删除**。Migration 021 → 029。验证：typecheck 零错误、214 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。**2026-08-28 新增：新手教程重写（6 步聚光引导覆盖大模型两种配置/工作区/资料库/三步生成）+ 启动自动预制「测试任务（仅作为演示）」（对话/汇编[矛盾+二次改动]/初稿）+ 修复规范列表点击跳错条目；验证 typecheck 零错误、176 项单测、生产构建通过。**待用户实测：真实 Provider 的 AI 细读/矛盾标注/语义补全、三步端到端、基于汇编生成初稿、按步骤默认模型生效。**Phase 6.7（测试/文档/发布）已完成**（文档同步至三段式现状、README 更新、PLAN.md 标记完成并将 Phase 5 重命名为 **Last Phase（收尾阶段）** 避免序号歧义；验证 typecheck 零错误、单测 190 项通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功）。**剩余 Last Phase（收尾阶段，原 Phase 5）：Windows 安装包 / 端到端演示 / 文档整理。**

## 设计决策（要点，按时间倒序）

- **补齐「重新生成汇编」入口，并删除两个"传错查询"的网页材料入口（2026-09-12 用户裁定）**：① **补入口**：已有汇编的任务此前**没有任何重跑生成的路径**（悬浮面板在已有汇编时只给「与汇编对话」，`compilation.regenerateBtn` 是无引用死文案），用户只能新建任务。现于「与汇编对话」面板底部加**「重新生成汇编」**+ 二次确认（`ConfirmDialog`），确认后调用同一条 `compilation:generate` 管线：网页材料沿用 A1 已锁定的一批（不重新抓取），产出是**一版新汇编**（新 id / 新对话与版本历史；旧版留库但界面只显示最新一版）。**教训**：界面文案（i18n 里的按钮字）不等于入口存在——写引导语前必须确认按钮真的被引用。② **删入口**：`compilation:adoptWebMaterials`（纳入新材料）与刚删的 `compilation:refreshWebMaterials` 同属"界面入口把**原始撰写要求**传给网页检索"，而正文精过滤 `matchesExact` 要求**原样包含整个检索词**（「高中学校设置」7 字长词）→ 必然 0 篇落库，界面还看不出原因；两者一并删除，后端抓取/锁定能力保留给生成管线。**网页材料集合自此只由生成管线管理**（首次落定 + 后续复用），面板只读显示「已锁定 N 篇」。
- **「重新检索网页材料」当日引入当日删除；网页检索存在"粗筛口径 ≠ 精过滤口径"的既有隐患（2026-09-12 用户实测）**：① 该功能（`compilation:refreshWebMaterials` + 按钮 + `clearPinnedWebMaterials`）对外是"重新发现/排序/抓取/重新锁定"，**先清空后抓取**。用户点击后只得到「锁定 **0** 篇（标题命中 495 篇，因上限未采用 0 篇）」，还把那 80 篇锁定材料清空了（`task_web_materials` 变空表）。**根因是口径不一致**：标题粗筛 `filterArticlesByQuery` 用 `matchesAny`（bigram，宽松），正文精过滤 `matchesExact`（**必须原样包含整个检索词**）；界面入口传的是**原始撰写要求**，`extractTopicTerms` 取到「高中学校设置」这个 **7 字长词** → 495 篇候选**没有一篇**正文原样含这 7 字 → 0 篇落库，而 `skippedByCap=0`、界面完全看不出是精过滤丢的（生成管线平时不暴露，是因为它传的是大模型提取的**短关键词**）。**处置**：**删除**该功能及其入口与文案（保留只读 `compilation:webMaterials` 与「已锁定 N 篇」提示）；`compilation:adoptWebMaterials`（纳入新材料）**保留但同样受此影响、实际不可用**，待统一改造。**教训**：新增一个"对外动作"前，必须把**它传入的查询与下游各道过滤的口径对齐**跑一遍真实数据，而不是只看"能不能点"。② 同时查明并记录一个产品缺口：**软件里没有「重新生成汇编」按钮**（`compilation.regenerateBtn` 是无引用死文案），已有汇编的任务只能新建任务重跑——待用户裁定是否补入口。
- **「重新检索网页材料」入口必须脱离"生成模式"（2026-09-12 用户实测找不到按钮；同日随功能一并删除）**：用户重启应用后按我的指引找不到该按钮——根因是**入口的渲染条件本身是错的**：提示条只写在悬浮面板的**生成模式**分支（`generatingMode = 还没有汇编 || 正在生成 || 生成中断`），而 A1 又让「重新生成汇编」**先按旧集合复用**，于是"换一批材料"的入口事实上够不到（且提示条数据来自 `lastWebScan` 会话状态，重启后为空，什么都不显示）。**修复**：① 新增只读 IPC `compilation:webMaterials`（`{taskId} → {pinned}`）查持久化锁定篇数，渲染层在任务切换 / 生成完成 / 纳入 / 重算后刷新；② 提示条抽成 `webInfo` 节点，**生成模式与对话模式都渲染**（无 `webScan` 时按持久化篇数显示「本任务已锁定 N 篇」，有则优先显示本次生成统计）；③ **生成进行中禁用**「纳入新材料」/「重新检索网页材料」（此前只互相禁用，边生成边抓取会互相干扰）。**教训（写进约定）**：给用户新增一个"补救入口"时，必须核对它在**用户真正需要它的状态**下是否可见——本轮的错误是"入口只在另一个状态出现"。
- **网页抓取上限不能再当"相关性取舍"用：修打分口径 + 放宽上限 + 材料集合可重算（2026-09-12 实测复议）**：用户复跑同一主题只得 99 段（上一轮 125 段）。逐段核对真实库：**网页段落 77 → 4 段**、本地文件段落 48 → **95 段**；缺失的 108 段里"换说法/合并"29 段（27 段来自本地年鉴，内容没丢），"这一轮完全没有"71 段中 **69 段来自网页**；76/108 段明确含「高中/中学/完中/普高/高中部」，丢失的是滨海新城福州三中滨海校区/分校、融侨（赛德伯）国际双语学校高中部、长乐三中恢复高中办学、长乐五中申报省级达标校等**完全切题**的内容，且新汇编完全没有 2019/2026/2011 年的材料。**两条根因**：① `WEB_FETCH_MAX_ARTICLES=80`（第二批引入）把抓取量从 477 篇压到 80 篇、**两轮重合 0 篇**，切题网页正文从 94.1 万字掉到 9.4 万字（−90%）；② **排序口径失效**——筛选 `matchesAny` 用 bigram，排序 `rankArticlesByQuery` 却用完整子串 `title.includes(term)`，而检索词是长词/空格拼接的关键词串，实测 477 篇与 80 篇标题打分**全部 0 分**，排序退化为保持清单原序（上限 = 按清单顺序瞎取）。**丢失内容从未进入管线**，不是大模型判无关。**三项修复**：**A** `extractTopicTerms` 对空格拼接的关键词列表**逐词**取词（此前抹掉空格拼成一整句），`rankArticlesByQuery` 改用与筛选一致的 **bigram 重叠计数 + 完整词加权**，领域下位词不参与排序；**B** 上限 80 → **300 篇**、40 万 → **150 万字**（A1 锁定后每个任务只付一次抓取代价）；**C** 新增 `compilation:refreshWebMaterials` + 面板「重新检索网页材料」，首次落定不理想时不必新建任务。**⚠ 长期告诫**：只要上限小于标题粗筛候选数（真实站点同一主题 495–3,293 篇），标题排序必然排除"标题不含主题词、正文却切题"的建校类文章（实测上一轮 42 篇有用材料在上限 300 下仅 5 篇入选）——**相关性取舍必须靠正文**（抓取时正文精过滤 + 保守闸门 + 细读窗口），上限只是成本保险丝。验证：typecheck 零错误、**264/265 单测通过**（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **网页资料库并入修志流程·第三批（2026-09-12，用户裁定范围 A1、C、D、E1–E3，B 删除）**：第二批之后按用户确认的清单落地六项，**B（生成前给出材料清单并允许逐篇排除）按用户要求从计划删除**。① **A1 网页材料集合在首次生成时落定**：Migration 039 新增 `task_web_materials`（主键 `(task_id, source_id)`，两个外键 `ON DELETE CASCADE`）；首次生成把本轮实际采用的网页来源锁定到任务，此后**重新生成默认复用同一批**（`listPinnedWebMaterials` 并回检索范围），期间站点新命中文章**只统计数量**（`collectSiteCandidates` 只发现不抓正文 → `webScan.reused` / `newCandidates`），用户点「纳入新材料」（IPC `compilation:adoptWebMaterials`）才抓取入库 → 锁定 → `ensureSourcesIndexed` 补索引。**取舍**：不自动跟随站点变化——否则"重新生成"会悄悄换材料，污染版本差异与矛盾编号。**已知限制**：面板提示取自最近一次生成的 `webScan`（会话内状态），重启软件后需再生成一次才重新显示，复用行为本身不受影响。② **C 来源本地快照 + 抓取时间**：新增只读 IPC `sources:getSnapshot`（读库里已存正文、**不联网**，上限 200,000 字并标注截断），卡片显示「抓取于 …」并可查看快照，按本次检索关键词**高亮命中片段**（空白容错匹配）。③ **D 伪矛盾收敛**：矛盾扫描提示词补「**以下情形不算矛盾，不要归组**」清单 + 「只有**同一时点、同一口径**下数据/时间/地点/主体/结果明确不同才算矛盾」+ 要求利用时间字段；矛盾说法行显示时间供人工核对。④ **E1**：`WebFetchStats.siteErrors` 进生成汇总（「另有 N 个站点检索失败」），不再静默。⑤ **E2**：无标题页面用 `host（页面无标题）`（`fallbackTitleFromUrl`）；`inferYearFromSource` 对**像 URL 的标题**不再采信（`looksLikeUrl`），避免 `t20251203` 被误当年份。⑥ **E3**：快照正文 <500 字时标注「正文偏短，可信度存疑」（`shortText`）。验证：typecheck 零错误、**261/262 单测通过**（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功（CSS 123.84 kB / JS 4,182.12 kB）；**Migration 039 真实库副本演练**：38 → 39，484 来源 / 170,689 分块向量 / 3 汇编 / 272 段落 / 14 矛盾 / 2 回收站条目零变化，`integrity_check=ok`、外键违规 0、重复执行幂等。
- **向量索引重建：可持久化进度 + 中断续跑 + 提速 8 倍（2026-09-12）**：用户实测「重建极慢、切走再回来进度就没了」，三项一并解决。① **进度持久化**：重建状态（`running/interrupted/done` + 开始时间 + 本轮待索引篇数）写入 `settings.index_rebuild`；进度由「开始时待索引数 − 当前未就绪数」实时算出，**跨页面切换与跨软件重启都可见**（界面不再用组件内的 `reindexing` 局部状态，改为由持久化状态推导）。② **幂等 + 续跑**：只处理 `index_state != 'ready'` 的资料（已索引的不重做）；启动时把上次被强杀时停在 `indexing` 的行**改回 pending**（否则它们永远不再被索引），并把残留的 `running` 记录标为 `interrupted`；界面据此显示 **「继续重建」**，再点只跑剩余部分，且沿用同一轮的分母/开始时间，百分比单调前进（50% → 100%）。③ **提速 8 倍（实测 133ms → 17ms/分块）**，两条独立原因：**padding**——transformers.js 会把一批补齐到该批最长序列，原样顺序批 5/批 24 = 162/481ms/条，而**按长度排序分组**后 23ms/条（新增 `planEmbedBatches`，索引阶段一次性把整篇分块交给 `embedTexts`，由它按长度分组投喂，返回顺序按原始下标还原）；**WASM 多线程**——`ort.env.wasm.numThreads` 原写死 1，实测 1/2/4/8 线程 = 130.5/86.7/44.7/24.3ms/分块，现由 `embed-config.ts` 把「核数 − 1」在 Worker 池（≤3）间均分、上限 8（32 核 → 3×8），线程不可用时**自动回落单线程重试**（否则每篇都失败）。④ **可观测**：`getEmbedEngineStats()` 透出池大小/实际线程数/Worker 出错与回退次数，经 `rag:indexStatus` 在设置页显示——这条至关重要：**Worker 失败会静默回退单线程**，第一版排查时正是靠 `workerCalls:10 / workerErrors:10 / directFallbacks:10` 才发现"看起来在跑、其实全在单线程"。真实库副本演练：6 篇真实网页资料（249 分块）**4.1 秒**跑完（17ms/分块；此前同规模 100 秒），`workerThreads:8`、零回退；中断续跑演练 50% → 只跑剩余 3 篇 → 100%，再次点击 `queued=0`（不重做）。按 484 篇估算整轮重建约 **5-6 分钟**（原约 45 分钟）。验证：typecheck 零错误、**255/256 单测通过**（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。

- **网页资料库并入修志流程·第一批（2026-09-10，用户要求评估后开工）**：用户问「加入网页资料库后会对修志流程造成哪些问题」并举例「网页不能像年鉴一样按年份 −1 兜底」。逐条核对代码后确认三条高严重度问题并已修复：① **年份兜底错年**——`inferYearFromSourceTitle` 原先对任何含年份的标题一律 −1，对新闻标题是错的（《2021年全区教育工作总结》→ 2020；标题退化为 URL 时 `t20251203` → 2024），且记为 inferred、界面不提示，属**静默错年**。新增 `inferYearFromSource({title,kind,publishedAt})` 按依据分流：年鉴/年报类标题 → −1（`title-yearbook`，由 `isYearbookLikeTitle` 判定）；其它标题年份 → **原样采用**（`title`）；网页来源无标题年份 → **发布时间**（`published`）；都推不出 → 保持「时间待核」。**副作用（须知）**：本地文件《2019年教育统计表》现按 2019 年（原为 2018）——同一条原则的自然结果。② **发布时间丢失**——网页发布时间此前只存 `web_site_articles.published_at` 供清单排序，抓成 `sources` 后丢弃；**Migration 037** 给 `sources` 加 `published_at` 并按 URL 从 `web_site_articles` 回填，`importSiteArticle` 落库写入、304 复用、老行顺手补齐。③ **网页资料从未进向量索引**——`enqueueIndex` 只被文件导入/手工加网址/工作区对账调用，网页资料库漏了，`index_state` 恒为 pending，保守闸门里"字面无关但语义相关"的低阈值向量兜底（`RECALL_VEC_MIN=0.1`）对网页**完全失效**；现改为抓取即入队，并在生成前用 `ensureSourcesIndexed`（预算 120s）等待就绪、超预算记日志（本轮只能靠词法命中）。**真实库副本演练**：35 → 37，7 份资料 / 2 汇编 / 147 段落零变化、0 份文件来源被写入发布时间、`integrity_check=ok`、外键违规 0；该库目前**没有任何网页材料**（url 来源 0、web_site_articles 0），故回填命中 0 行，本批对其属预防性改动。**未做（第二/三批）**：跨来源近似重复合并、抓取篇数/字数上限、无 Provider 时跳过抓取并改用闸门后材料、网页材料集合落定与生成前清单、网页快照溯源。④ **向量索引为何全库失败（2026-09-12 查清并修复）**：用户真实生成后核对发现 `chunk_embeddings` 全库为 0、477 个网页来源与 5 个文件来源全部 `index_state='failed'`——即**向量语义兜底对所有来源从未生效**。根因**不是模型缺失**（`resources/models/bge-small-zh-v1.5/` 含 94 MB `model.onnx`），而是 `onnxruntime-node` 这个 **file: 依赖（`vendor/onnxruntime-node-stub`）没有被正确安装**：`node_modules/onnxruntime-node` 与 `node_modules/@huggingface/transformers/node_modules/onnxruntime-node` 都是**空目录**（`npm ls` 报 invalid），于是 transformers 的 node 构建 `import 'onnxruntime-node'` 失败 → `pipeline()` 抛错 → 每个来源都 failed。**修复**：`npm install` 重新物化该依赖（现为指向 vendor 的符号链接，`npm ls` 正常）；实测真实网页资料 3,731 字 → **42 个分块、5.6 秒**索引完成，`index_state='ready'`、`chunk_embeddings` 有行。**并补上可诊断性**：Migration 038 给 `sources` 加 `index_error`，`indexSource` 失败即落库原因并打日志（此前只有 console.error，界面无从判断）；`embed.ts` 的报错文案不再只让用户"检查模型目录"，而是同时指出引擎/依赖不可用的可能并附原始错误；新增 IPC `rag:indexStatus` / `rag:reindex` + 设置页**「本地检索索引」**区块（状态、已索引/总数/失败数、最近失败原因、「重建索引」按钮，重建走后台串行队列、界面每 2 秒轮询进度）。**遗留**：存量 failed 行无历史原因（本次迁移不回填），点一次「重建索引」即可全部转 ready（484 篇串行，按实测约 5.6s/篇估算需数十分钟，可在后台进行）。
- **Phase 7.7 收尾清理：删除「大模型修正」与卡片回收站，收口无入口通道（2026-09-10）**：两项机制都被后续重构架空，本轮一次性拆净。① **大模型修正**：Phase 7.2 的整合提取取代「提纯 + 修正」两趟后，`compilation_repairs` 不再有新记录；Phase 7.6 的连续文档改造又抽掉了卡片徽标与详情弹窗。删除范围＝渲染层徽标/弹窗/工具栏统计 + `Compilation.repairs` + `compilation:repairs:revert/apply` + `compilation-repairs.ts` 仓储 + `CompilationItemInput.repair` + `CompilationOutputItem.repair`。② **卡片回收站**：软件内最后一条逐段删除入口随「人工修改模式」在 7.5 三轮验收中被删除，此后 `compilation_card_recycle_bin` 只减不增 → 回收站收缩为**仅矛盾**（`compilation_recycle_bin`），`deleteCompilationItem`/`deleteCompilationItemsByIds` 改为硬删除，撤销快照去掉 `repairs` 字段。③ **Migration 036**（破坏性）：`DROP TABLE compilation_repairs`、`DROP TABLE compilation_card_recycle_bin`；迁移账本保持只增不改（029 的建表/改表条目原样保留）。**真实库副本演练**：35 → 36，130 条修正记录 + 6 条卡片快照被清除，6 汇编/737 段落/10 矛盾/6 条矛盾回收站条目零变化，`integrity_check=ok`、外键违规 0。④ **无界面入口的通道**：`compilation:updateItem`/`deleteItem`/`version:diff`/`version:restore` 删除（含 preload 与 d.ts），仓储函数保留供单测/演示使用；`restoreCompilationFromVersion` 保留为版本恢复原语（当前无调用方，注释已注明）。⑤ **演示数据与本地降级也走段落模型**：`demo-task.ts` 原先直接调 `insertCompilationItems`（旧卡片模型）写演示汇编，导致演示任务缺年份分节、来源圆标与版本基线；无 Provider 的本地降级路径同样如此。两者改为 `upsertCompilationParagraphs` + `ensureCompilationSources` + `snapshotCompilationVersion`，与真实生成管线同一套落库口径。验证：typecheck 零错误、**243/244 单测通过**（1 项 watcher chokidar `unlink` 环境失败为既有问题）、生产构建成功。
- **资料卡片「提纯」阶段（2026-09-08 新增；2026-09-10 实测后收紧口径 + 句读吸附 + 并行）**：管线改为 `AI 分窗细读 → **提纯** → 修正 → 卡片矛盾扫描`。动因：真实数据实测（「高中教育3」，191 张卡 / 48,063 字）显示，因切片策略是「整段成卡」，**相关句仅约 31%（14,914 字），51 张卡（27%）通篇无一句相关**，噪声稀释了矛盾检测与初稿材料。提纯 = 让大模型对整段卡片做**逐字摘录**（只保留与主题相关句段）。两条硬约束：① 严格子串 + 本地「去空白归一化」校验并回取原文真实子串，校验失败丢弃、整卡失败则保留原卡（宁多勿漏）；② 由提示词判定、**代码侧不做内容拦截**。**置于修正之前**（用户选定）：修正的「标记 + 回退」与最终卡片一对一、无需迁移记录，且修正/矛盾扫描输入从整篇降到约 1/3（净增成本仅一趟 pass）。**黑箱定位**（用户选定）：不加列/不加表、不展示提纯前原文、不支持单独回退（退回用「重新生成汇编」）、被舍弃内容不入回收站。失败/超预算时未提纯卡片**按原样保留**并透出 `purifyScan:{ok:false}`；纳入断点续跑（`phase: window|purify|repair|contradiction`）。窗口级矛盾说法经 `mapWindowGroupsThroughPurify` 映射到片段（否则落库按 excerpt 匹配不到卡片会整组丢失）。离线估算：48,063 字 → 约 16,197 字（34%），卡片 191 → 166~214 张，44~51 张整卡丢弃。**2026-09-10 实测复议（「高中教育4」253 张卡 / 48,213 字，相关字占比只从 31% 升到 40%）后三项改造**：① **口径收紧为「写通测试」**——提示词从「只要有可能的联系就保留、拿不准的一律保留」改为「设想这段文字会不会写进题为《主题》的志稿正文」，同为某大领域但对象/学段/业务不是本主题的（幼儿园、小学、义务教育、教师聘任表、文明校园评比等）一律删除（用户选定；旧口径下实测只删掉约 1/4 材料）；② **本地句读吸附** `snapSpansToSentenceBounds`——片段起点左扩到句读边界、终点右扩到句末标点（含收尾引号括号），**只外向不内向**（不丢字符，上限 `PURIFY_SNAP_MAX_CHARS=200`），修掉实测 43 张起点不在句读边界（如 `心扑在…` 被截掉「一」）+ 11 张结尾半句的残缺卡片，残留的缺主语/指代不明交修正阶段补全；③ **性能与可观测**——批次 30→50 张 / 12000→18000 字、预算 600s→900s、调用**由串行改为按 Provider 并发并行**（实测串行 8 批 610s，而细读 19 窗 1331s 靠 4 路并发墙钟仅 411s）、**去掉 `reason` 字段**（黑箱用不到、占输出 25~30%）、漏答卡片单独重问一次 + 解析失败换温度重试一次、逐批诊断日志（输入/保留字数、整卡丢弃、漏答、原样保留、校验失败、吸附处数、重试次数）与 `purifyScan.passthroughCards` 透出。验证：typecheck 零错误、228 项单测通过（总计 229，1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。**待用户用真实 Provider 复跑复核：相关字占比目标 ≥65%、字数目标 ≤30,000、边界不合格 0、提纯全部批次跑完且墙钟 ≤200s。**
- **资料卡片「大模型修正」默认应用 + 标记承载 + 前移到矛盾检测之前（2026-09-08）**：用户需求变更——① 原「二次加工/语义补全」需用户逐条裁定「采纳/不用」，现改为**默认全部应用**，仅在卡片上留「✎ 经过大模型修正」标记；点标记弹窗展示**修正前原文 + 修正后文本 + 修正理由**，并提供「回退到修正前」（状态 applied→reverted，卡片文本还原；标记转灰「↺ 已回退到修正前」）与「重新应用修正」（reverted→applied）。② 修正阶段**从「生成完成后的独立扫描」移入生成管线内，位于 AI 分窗细读之后、卡片矛盾扫描之前**（先让卡片语义完整再查矛盾）；`runRepairPhase` 与窗口/矛盾阶段同级，纳入 `resumeStore.phase`（window→repair→contradiction），大模型异常即中断并由「尝试继续」续跑（只重跑未完成批次）。③ **回收站不再收录该类条目**（由 3 类降为「资料卡片 / 矛盾」2 类，`compilation_repair_recycle_bin` 随 Migration 029 删除）；被删卡片仍把其修正记录快照进 `compilation_card_recycle_bin.extra`，随卡片恢复。④ **时间戳自动补齐不算修正**（静默、无标记、不可回退）——但仍随管线前移，故落库按时间排序自然正确。其他要点：修正记录通过与卡片**同事务**写入（`CompilationItemInput.repair` 随卡片流经来源过滤/按时间排序，避免标记错位）、分批（30 张/12000 字）串行 + 单批 300s 超时 + 整阶段 900s 预算、窗口级矛盾的 variant 文本同步改写（`remapRepairedVariantExcerpts`，否则落库按 excerpt 匹配不到卡片会丢整组）、IPC 由 `repairScan/repairs:list/repairs:decide` 改为 `repairs:revert/repairs:apply`。**术语**：代码/表名沿用 `repair`，界面文案称「大模型修正」。验证：typecheck 零错误、214 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。**2026-09-10 实测后收紧（「高中教育4」253 张卡中 12 张 ts 只有月日如 `5 月 19 日`/`7—9 日`，且全部没有修正记录、0/12 自身正文含 4 位年份）**：① 时间补齐范围由「时间显示为『无』」扩为「**缺少年份**」两种情形，提示词明确「志书的时间标注**必须含年份**」，依据上下文与来源年鉴年份（如《长乐年鉴2019》→ 2018 年）推断，**确无依据时不给 ts、不得编造**；本地以 `hasYear`（4 位年份）/`shouldFillTs` 两道校验取舍——模型给的 ts 不含年份一律不采纳（计 `tsRejected` 诊断），卡片已有含年份的 ts 不覆盖，**缺年份的旧值允许被覆盖**（`applyRepairOutcome` 同步放宽；ts 补齐仍属静默、不落记录）。② 残缺判定补充「句子起点/终点不完整（从词中间/半句话开始、以半句话结束）」「缺少主语/谓语/宾语」「指代不明（他/其/该校/该年而摘录与上下文均看不出指代对象）」，并明确「**优先补全，不要删**——只有确实无法补全时才可删去残缺部分并在 reason 说明」（实测旧实现中模型多次直接删掉残缺数据，等于丢材料）。验证：typecheck 零错误、228 项单测通过（总计 229，1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **写作规范 skills 替代范本（2026-08-13）**：范本（体例参考）重构为"写作规范"（通用规范默认注入 + 部类细则按标题匹配/智能匹配/手动选择），`writing_skills` + `writing_tasks.skill_ids`（Migration 014）；范本相关 UI/IPC 移除。
- **网页资料库（2026-08-11/13）**：注册站点 → BFS 栏目遍历发现文章清单（限 20 页/深度 2）→ 标题 bigram 宽召回 → 正文精确子串精过滤 → 增量抓取正文落库为任务绑定缓存（`sources.task_id`，删任务级联清理，不进资料库列表）；标题粗筛用"领域下位词兜底表"（当前仅教育→学前教育核心词），严格收窄避免政务学习类新闻误召回。
- **矛盾检测三次调用链路（2026-08-10/11）**：检索后预扫描（低温度 + 温度阶梯 0→0.3→0.7 重试、主题聚类 + 整组窗口并发扫描、空结果收敛）→ 生成注入"严禁合并/折中 + `【矛盾#N】` 标注" → 定位审查（回填 draftQuote/merged/inDraft/replacements）。扫描视野收敛为"撰写实际用到的检索文段"（用户确认取舍）。**采纳 = 纯本地替换**（from=draftQuote → to=replacement，无 LLM 调用），编辑器 setContent 进 undo 历史实现撤销兼容。
- **产品范围收敛（2026-08-11）**：删除版本迭代与管控；"事件缺失补充"暂不在范围。
- **检索策略演进（2026-08-07）**：取消 Top-N/截断限制，改为"过滤式"——词法 score>0 或向量余弦 ≥0.3 的段落全部保留；标题行（≤12 字短语等）一律剔除（曾致初稿只有标题无正文的根因）。
- **向量推理后端（2026-08-06/09）**：Windows System32 的 onnxruntime.dll 抢先加载导致 onnxruntime-node 原生绑定不可用 → `vendor/onnxruntime-node-stub` 转发到 onnxruntime-web WASM（单线程）；嵌入推理移到 worker_threads（embed.worker.js），主进程自动回退直接推理；索引改为后台串行队列（列表秒出，向量后台补）。
- **工作区资料库（2026-08-06/07）**：全面替换"导入转存"；指纹（sha256+mtime/size）做映射锚点（移动/重命名不丢 id/标签/摘要）；chokidar 500ms 防抖增量 + 聚焦/进资料库/每分钟全量兜底（mtime/size 快筛，开销低）；工作区删除文件 → 直接删库（级联清理），软件内删除 → 回收站；对账只做"文件系统 → 数据库"方向防环路。
- **全程可溯源 / 信源白名单 / 人机协同 / 本地优先**：见"架构原则"。
- **技术选型（2026-08-03）**：Electron + React + TypeScript。
- **编辑器选型（2026-08-05）**：TipTap + tiptap-markdown，初稿以 Markdown 存储、连续显示。
- **版本管控（已删除，2026-08-11）**：原"第 n 稿"版本单元方案随产品范围收敛移除。

## 路线图

1. Phase 1：项目基础（脚手架、共享契约、本地数据库）——已完成
2. Phase 2：资料收集闭环（工作区资料库 / 标签 / 网页资料库 / 写作规范）——已完成
3. Phase 3：撰写闭环（LLM 接入 / RAG 检索 / 初稿生成与来源标注 / 矛盾检测与取舍）——已完成
4. Last Phase（收尾阶段）：验收与打包（Windows 安装包、端到端演示、文档）——**待进行**

详细任务和验收标准位于 `PLAN.md`，本文件不重复记录任务级进度。

## 近期记录（摘要）

> 
> **2026-09-10（Phase 7.7 收尾清理：删「大模型修正」与卡片回收站 + Migration 036 + 无入口通道 + 演示/降级走段落模型）**：按用户「推送 7.6 后进入 7.7 收尾清理」的指令，分 5 个提交完成（A/B1/B2/C/D）＋本轮 E：
> 1. **A**：删 `compilation-adjust.ts` 与 `compilation:adjust` 链路（已被 7.5 的对话编辑取代）。
> 2. **B1**：删 `purify-service.ts`/`repair-service.ts` 与 `compilation-service.ts` 中已无人调用的 `runPurifyPhase`/`runRepairPhase` 及遗留 import、`CompilationResumeState` 的旧字段。**测试数从 270 掉到 248 不是回归**——两个服务文件各自带着约 24 项内联测试。
> 3. **B2**：修正链路端到端下线——渲染层徽标/详情弹窗/工具栏统计/`onDecideRepair`、`Compilation.repairs`、`CompilationRepair` 类型、两个 IPC 通道 + preload + d.ts、`compilation-repairs.ts` 仓储、`CompilationItemInput.repair`、`CompilationOutputItem.repair`、相关 i18n 与 CSS（`.compilation-chip--fix`/`.compilation-fix*`）。
> 4. **C（破坏性）**：**Migration 036** `DROP TABLE compilation_repairs` / `DROP TABLE compilation_card_recycle_bin`；回收站收缩为**仅矛盾**（`CompilationRecycleBinItem` 去掉 card 分支，restore 响应只剩 `contradiction`）；`deleteCompilationItem`/`deleteCompilationItemsByIds` 改硬删除、去掉卡片入站快照；`compilation-undo.ts` 快照去掉 `repairs`；来源移除提示去掉「N 条大模型修正」（`deletedRepairs`/`repairCount` 一并从契约、preload、`App.tsx` 移除，`App` 四处内联类型改用共享 `WorkspaceSourceRemovalPending`）。**真实库副本演练**：35 → 36，`compilation_repairs` 130 条、`compilation_card_recycle_bin` 6 条被清除，6 汇编 / 737 段落 / 10 矛盾 / 6 条矛盾回收站条目零变化，`integrity_check=ok`、外键违规 0。**用户需知**：升级后回收站只剩矛盾一类，此前躺在卡片回收站里的 6 张卡片无法再恢复。
> 5. **D**：删四个无界面入口的通道 `compilation:updateItem`/`deleteItem`/`version:diff`/`version:restore`（含类型、invoke map、preload、d.ts）；清掉因此变成死代码的 `compilationIdOfItem` 与主进程 `compilation-diff` import（diff 函数本体保留，`doc-edit-runner` 仍在用）；仓储 `updateCompilationItem`/`deleteCompilationItem`/`restoreCompilationFromVersion` 保留（前者有单测覆盖，后者加注释说明当前无调用方）。
> 6. **E**：删 `.compilation-cards`/`.compilation-card*` 死样式（保留 `@keyframes compilation-card-locate`，`.compilation-para.is-located` 仍在用）；`docs/{ui-architecture,data-model,shared-contracts}.md` 全量对齐（2.24/2.25 两节改写为「已删除 + 原因 + 演练数据」，IPC 清单标注删除项）；新手教程文案去掉「大模型修正标记/事实卡片」并改写生成流程描述；**`demo-task.ts` 与本地降级路径改走真实段落模型**——原先两者都直调 `insertCompilationItems`（旧卡片模型），演示汇编因此缺年份分节、来源圆标与版本基线，现统一为 `upsertCompilationParagraphs` + `ensureCompilationSources` + `snapshotCompilationVersion`（降级路径还经 `parseTimeLabel` 写结构化时间）。`AGENTS.md`/`PLAN.md` 基线与迁移范围同步（001–036）。
> **验证**：typecheck 零错误；vitest **243/244 通过**（1 项 watcher chokidar `unlink` 环境失败为既有问题）；生产构建成功（CSS 125.06 kB、JS 4,162.86 kB，均较 7.6 变小，与删除量一致）。**未做**：真实 Provider 端到端复跑（切片 F，需用户操作）。提交：`a42e3a1`(A) `336c2c4`(B1) `777e2db`(B2) `77e3647`(C) `34cab09`(D) + 本轮(E)。
> **2026-09-10（Phase 7.5 三轮验收：删除「人工修改模式」、撤销栈只登记对话编辑、对话修改后自动复核「采纳 / 回退」）**：用户复验通过后改变了两条产品决策，本轮按新需求收敛交互模型：
> 1. **删除「人工修改模式」及其前后端全部实现**。新需求：软件内不再逐段手改，改为**导出资料汇编到本地修改**（好处是核对来源存疑时可随时回软件内查看）。删掉：Migration 035 `DROP COLUMN compilations.manual_edit`（034 当天引入当天删除，**保留 034 条目不改写迁移账本**，同类先例见 Migration 026 删 E11 keywords 列）、`Compilation.manualEdit`、仓储 `setCompilationManualEdit`、IPC `compilation:manualEdit`、preload `enterCompilationManualEdit`、`CompilationStep` 的 D5 门槛（徽标 / 「开始人工修改」/ 不可逆二次确认弹窗）、段落悬停「编辑」「删除」与编辑弹窗、`WritingWorkspace` 的 `handleStartManualEdit`/`handleUpdateItem`/`handleDeleteItem`，以及配套 i18n 与 CSS（`.is-manual*`/`.compilation-manual-warn`/`.compilation-para__actions`）。**真实库副本演练**：34 → 35，列被删除、其余列逐一相同，汇编 5 / 段落 664 零变化，`integrity_check=ok`、外键违规 0。
> 2. **撤销栈只登记对话编辑**（用户实测"点排序按钮也会点亮撤销"）：`pushUndo` 现在**只在 `runDocEdit`**；其余会改内容或顺序的路径（排序 / 矛盾取舍 / 回收站恢复 / 修正回退与再应用 / 目录级编辑 / 版本恢复 / 汇编调整）改为 **`clearUndoStacks(cid)` 作废**撤销栈而非压栈——快照是整个汇编的状态，若两次对话编辑之间发生了别的改动，弹栈会把那些改动一并回滚。这些路径同时不再记版本。
> 3. **取消「与修改前的上一版对比」按钮 → 自动复核**：对话修改成功后主进程在响应里直接返回**本次修改前后**的差异（`readParagraphSnapshot(改前) vs 改后` → `diffParagraphVersions`，**不靠版本号推算基线**——版本会被回退、会被裁剪到 2 版，用它当基线会算错），渲染层据此**自动进入对比模式**并显示 **「回退」「采纳」** 两个并列按钮：采纳 = 保留改动退出；回退 = 调 `compilation:undo` 弹出最近快照、**不产生新版本**、退出复核。复核态下禁用工具栏撤销/恢复/排序（唯一出口是复核条）。版本下拉、对比开关、`handleSelectVersion` 一并删除。
> 4. **对话改时间后同一次操作内按时间重排**（承接上一轮"改完时间自动重排"，原先挂在已删除的手工编辑上）：`runDocEdit` 检测到"时间标签变了或有新段"即 `sortParagraphsByTime` 重排后再落库；纯 `move`（用户明确要求调序）不触发，避免覆盖用户的调序意图。
> **验证**：typecheck 零错误、**260/261 单测通过**（1 项 watcher chokidar `unlink` 环境失败为既有问题）、生产构建成功；产物同时变小（CSS 127.15 → 125.10 kB、JS 4,175 → 4,166 kB），与删除量一致。
> **2026-09-10（Phase 7.5 用户验收整改：撤销栈、高亮残留、人工修改模式持久化、改时间自动重排、数字校验豁免）**：用户按清单验收后反馈「验收项基本通过」+ 1 个 bug 与 4 项改动，逐项落地：
> 1. **对话编辑无法被「撤销操作」撤销（真 bug）**：`doc-edit-runner` 落库前漏了 `pushUndo`，撤销栈顶仍是更早的快照，撤销会跳过这次对话改动 → 落库前补 `pushUndo(compilationId)`（与手动编辑/删除/矛盾取舍同口径）。`undoAvailable` 随 `compilation` 变化自动刷新，按钮随即可用。
> 2. **改动段底色变红且不恢复（真 bug，两个叠加原因）**：① 我把高亮挂在 `docChangedIds` 上且从不清理 → 底色永不消失；② 我用了 `--accent-soft`，而 **classic 主题的 accent 是暗红**（`#8c3b2e` / `--accent-soft: #f3e2dc`），所以显示成"红色"。改为**独立高亮 token** `--hl-changed` / `--hl-changed-soft`（青绿系，三套主题各给值，**刻意不跟随 accent**，与对比模式的 红=删除/黄=修改/绿=新增 都能区分；classic 主题尤其必要），并在 `CompilationStep` 内 **3.5 秒后自动清除**。
> 3. **人工修改模式落库持久化 + 不可逆**（用户裁定推翻原"会话内状态"）：Migration 034 `compilations.manual_edit INTEGER NOT NULL DEFAULT 0`、仓储 `setCompilationManualEdit`、IPC `compilation:manualEdit`、preload `enterCompilationManualEdit`；界面状态一律由 `compilation.manualEdit` 派生（删掉渲染层 `manualMode` state），**只增不减、无反向通道**，切换任务/重启保持。不记版本、不登记撤销栈（否则撤销能把它退回去，违背"不可逆"）。**真实库副本演练**：Migration 33 → 34，列 `INTEGER NOT NULL DEFAULT 0`，5 份存量汇编全为 0，汇编/段落数零变化，`integrity_check=ok`。
> 4. **改完时间自动重排 + 滚动定位 + 2 秒高亮**：`compilation:updateItem` 在 `ts` 变化时**同一次操作内**调 `reorderCompilationItemsByTs(cid,'asc')` 并返回重排后的完整 `compilation`（契约加可选字段；前端整体替换）——**不额外记版本**（用户视角是一次编辑）。前端 `refocus={id,nonce}` 滚动到该段新位置 + `.is-refocused` 高亮 2 秒（同青绿系）+ 工具栏排序图标复位为 ↑。配套修正：编辑弹窗**只提交真正改动的字段**（原来每次保存都带 `ts`，会把"只改正文"误判成"改了时间"而无谓重排、冲掉用户手调顺序）。
> 5. **用户明确要求改数字时不再校验**：op 加 `allowNewNumbers?: boolean`（`replace`/`insertAfter`/`replaceAll.paragraphs[]`），提示词明确"**只有用户点名具体数值时才能加**，其它情况一律不加，绝不可用它给推测/估算开口子"，本地校验见到该字段直接放行（用户裁定"大模型和软件都照做"）；解析层只认字面 `true`。用户对已知取舍的裁定：1/3/5/7 接受；**2 改为持久化**；**4 改为自动重排**；**6 改为豁免**；8（空正文校验）不需要。
> **验证**：typecheck 零错误、**261/262 单测通过**（1 项 watcher chokidar `unlink` 环境失败为既有问题）、生产构建成功。
> **2026-09-10（Phase 7.3/7.4/7.5：连续文档查看器 + 版本差异管控 + 悬浮对话框人机协同编辑）**：
> **7.3 连续文档查看器（右栏主体切换）**：右栏由「卡片列表」改为**一篇连续文本**——每段 = 段首时间徽标 + 正文 + **段尾来源圆标**，按 `year` 分节渲染年份小标题；圆标**可点击**（弹出「来源小卡」：编号/标题 + 该来源在本汇编的全部段落 + 打开原文），悬停用自绘 fixed 气泡（原生 `title` 在滚动容器里不可靠，实测踩过）；段级操作改为悬停显示；工具栏统计改为「N 段 / N 篇来源 / N 段时间待核」。
> **7.4 版本管控与差异高亮**：内容变更类操作各记一版（生成 `generate` / 对话 `llm-edit` / 手动 `user-edit` / 矛盾 `contradiction` / 导入 `import`），**恢复历史版本不记版本**；按用户 2026-09-10 两次简化——① 只保留**最近 2 版**（`pruneCompilationVersions(id, 2)`），② UI 只留**一个对比开关**（与"改动前的上一版"对比，无版本下拉、无恢复按钮，`compilation:version:restore` 主进程保留但界面不挂）。差异 = 段落级（added/modified/removed/unchanged）+ modified 段的行内**字级 LCS**（>400k 单元格降级为整块标记）；被删除的段带 **`beforeId`** 锚点，渲染时**插回它被删除前所在的位置**（用户要求）；「仅看改动」勾选后统一显示年份小标题（修掉"只有首段带年份标题"的观感缺陷）。**踩坑修复**：版本快照原先包含软删除段 → 矛盾取舍版本的变更统计全是 0，改为**只快照 `kept` 段**；`restoreCompilationSnapshot` 原先只写回旧 10 列，导致恢复后 9 个新段落列被清空（用户实测「按时间排序后年份标题消失、全部显示待补年份」的根因），已补齐 + Migration 033 数据修复（真实库副本验证：756 行 `year NULL=178 / unknown=178 / 无编号=148` → 全部 0）。
> **7.5 悬浮对话框与人机协同编辑协议**：新增 `doc-edit-service.ts`（协议纯函数：`parseDocEditOutput` / `validateDocOps` / `applyDocOps` / `buildDocEditMessages` / `resolveTimeForEdit` / `collectDocEditChange`）+ `doc-edit-runner.ts`（编排：乐观锁 → 用户消息先落库 → `chatCompletion(kind:'compilation-doc-edit', temp 0, 240s)` → 解析 → 逐条校验 → 应用 → **单次 `upsertCompilationParagraphs`（保留段 id）** → 记版本 → 写助手消息）。8 种 op（delete/replace/insertAfter/move/merge/split/setTime/replaceAll），校验含「段号存在 / 来源编号 1..N / **正文数字必须在该来源原文中找得到**（防幻觉，沿用整 token 口径）/ merge 仅同来源 / 不得删空整篇」；解析失败文档不变并报错。UI：右下悬浮圆按钮（自绘机器人）→ 可拖动/可最小化面板（历史气泡 + 输入框 + Ctrl/⌘+Enter；编辑中禁用并显示「正在修改汇编…」；错误态红条），改动后**高亮本次改动段并滚动到首个改动段**。**D5 手动编辑解锁**：`finalized` 前段落悬停**无任何编辑入口**且工具栏标注「仅可对话修改」；确认后出现「开始人工修改」+ **不可逆二次确认**弹窗。**旧链路收口**：左侧任务对话框后续消息改走**同一 `doc:edit` 后端**（`handleDocAdjust` 不再调 `compilation:adjust`），旧 `compilation-adjust.ts` 留待 7.7 删除。
> **验证**：typecheck 零错误、**258/259 单测通过**（1 项 watcher chokidar `unlink` 环境失败为既有问题，非功能回归）、生产构建成功。**待用户用真实 Provider 实测 7.5 示例场景**（「校区建设不属于这方面的内容，请你把校区建设相关内容都删掉」→ 相关段被删除、查看器即时更新并高亮）与错误路径（幻觉段号 / 跨来源 merge / 来源中不存在的数字 / 乐观锁冲突）。
> **2026-09-10（Phase 7.1/7.2：汇编从「资料卡片」改造成「连续文档」，管线合并为「细读筛选 → 整合提取 → 矛盾扫描」）**：用户提出六点需求并逐项裁定（D1 合并两趟为一趟 / D2 圆标 = 本汇编内首次引用顺序 1..N / D3 大模型以 ops 引用段 id 修改 / D4 结构化 year·month 本地排序 + 按年份分节 + 允许对话调序 / D5 **确认汇编前查看器无编辑入口，确认后「开始人工修改」+ 不可逆二次确认** / D6 废弃修正记录与卡片回收站，撤销恢复改为「上一版/下一版」）。
> **7.1（数据模型，纯新增）**：Migration 030 新增段落元数据 9 列（结构化时间、时间可信度、来源编号、证据引文、origin、revision、kind）+ 三张新表（`compilation_sources` 来源编号表、`compilation_versions` 版本历史、`compilation_messages` 汇编级对话）；Migration 031 回填旧汇编（按首次引用分配编号、解析年份、生成 v1 版本）。真实库副本演练：迁移前 maxVersion=29 / 5 份汇编 / 646 张卡片 → 迁移后无编号段 0、有年份段 611、时间待核 35、版本 5 个、零丢失、integrity_check=ok。新增 `compilation-document.ts`（时间解析 / 一段一行 markdown / 段落快照 / 变更统计 / 稳定排序 / 提取校验）+ 仓储 `ensureCompilationSources` / **`upsertCompilationParagraphs`（保留段 id）** / 版本与对话读写。
> **7.2（管线）**：细读提示词调轻为"只筛选、宁多勿漏、整段保留"；新增 `extract-service.ts` 完成**裁剪 + 补全 + 整合**，取代原「提纯 + 修正」两趟；三道本地硬校验（evidence 逐字、数字整 token 有据、时间可信度本地判定）失败即**降级保留原文整段**；成文阶段去重（近似重复仅在数字一致时合并，**数字不一致两段都保留**）并按 年→月→生成序 稳定排序；落库改为 `ensureCompilationSources` + `upsertCompilationParagraphs` + v1 版本；窗口级矛盾改用 `mapWindowGroupsThroughExtract`（相似度 <0.5 视为已裁掉、丢弃该说法）；契约 `purifyScan`/`repairScan` → 单一 `extractScan`。**旧阶段保留为 `@deprecated` 死代码，Phase 7.7 清理时删除**（先让新管线验收，再一次性拆旧）。测试当场抓出三个真实缺陷并修复：数字校验的"子串包含"后门（`2` 被 `2018` 蒙混）、近似重复"更长者胜"误替换、整批只有 `dropped` 被判无效。
> 验证：typecheck 零错误、**248/249 单测通过**（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。**待用户用真实 Provider 复跑核对对比表**（相关字占比 / 字数 / 段数 / accepted·degraded / 时间待核段数 / 矛盾组数 / 耗时）。
> **2026-09-10（提纯实测复议：口径改「写通测试」+ 句读吸附 + 并行提速；修正补齐缺年份 ts 与残缺句）**：用户用真实 Provider 复跑同类任务（「高中教育4」，与「高中教育3」同一资料库/同一指令）后提出三点：① 相关率虽升但仍大量无关内容；② 仍有卡片文字残缺（如 `心扑在学校发展事业上，团结带领学校领导班子和全体教师……2020 年 9 月，被省人力资源和社会保障厅、省教育厅评为"省优秀教育工作者"。`）；③ 提纯超出时间预算。我逐项核对本地库真实数据后定位根因（不猜测、全部有数据支撑）：
> - **提纯删除量只有应有量的约 1/3**——253 张卡 / 48,213 字，相关字占比 **40%**（高中教育3 为 31%，几乎无改善），总字数 48,213 与提纯前 48,063 基本持平；`compilation-purify` 8 次调用输入 85,319 字 / 输出 65,441 字，扣掉系统提示与卡片标签开销后**保留率约 70~75%**，而离线按句级估算应删掉约 2/3。根因三条：**提示词按旧口径写成了「只要有可能的联系就保留、可作为背景/对比/佐证」**（模型几乎不敢删）、**漏答/解析失败会整卡 passthrough 且完全没有日志**（看不见）、**第 9 批超 600s 预算整批原样保留**。
> - **残缺卡片不是修正阶段的问题，而是提纯/细读的片段边界没对齐句子**——181 张可在来源中逐字定位的卡片里 **43 张起点不在句读边界**（如 `心扑在…` 被截掉了「一」、前一字为「述/育/学/日」等）、**11 张结尾是半句**（如 `…被省高职院校录取 498 人`、`…贫困生`、`…1.50 万元`）；且这些卡片**都没有修正记录**，说明旧修正阶段的判定条件（表格切片/孤立短语/缺主谓宾）**根本没覆盖「句子起点终点不完整」**。另核实：253 张卡里**没有任何一张含 `……`**（用户引文中的省略号是用户自己的缩写）。
> - **12 张卡片 ts 只有月日没有年份**（`29 日`、`5 月 19 日`、`7—9 日`、`20—27 日`、`9 月 13 日`、`十三五规划期间`…），**全部没有修正记录且 0/12 自身正文含 4 位年份**；根因是旧提示词只处理「时间显示为『无』」。反证：66 条修正记录里 32 条 reason 涉及时间，模型明确写过「据《长乐年鉴2019(最新版).pdf》推定为 2018 年」——**有能力推断，只是没被要求**。
> - **提纯耗时异常的根因是我把批次写成了严格串行**：提纯 8 批 610s（单次 65~99s）串行，而细读 19 窗 1331s 计算量靠 4 路并发墙钟只有 411s；加上 `reason` 字段（黑箱永远不展示）白占 25~30% 输出、批次偏小（30 张/12k 字 → 9 批）。
>
> 经用户逐项确认后落地（4 项决策：A 写通测试口径 / C 本地吸附 + 修正兜底 / A ts 必须含年份 / A 性能全做）：① **口径收紧为「写通测试」**——「设想这段文字会不会写进题为《主题》的志稿正文」，明确「判定依据是志稿正文会不会用到它，而不是与主题有没有一点点关系」，同为某大领域但对象/学段/业务不是本主题的（幼儿园、小学、义务教育、教师聘任表、文明校园评比等）删除。② **本地句读吸附** `snapSpansToSentenceBounds`（起点左扩到句读边界、终点右扩到句末标点并吸收收尾引号括号，**只外向不内向**、上限 200 字）插在 `locateSpans` 与 `coalesceSpans` 之间，`resolvePurifiedSpans` 统一走「校验 → 吸附 → 合并」；残留的缺主语/指代不明交修正阶段补全。③ **提纯性能与可观测**——批次 50 张 / 18000 字、预算 900s、`runPurifyPhase` **按 Provider 并发并行**（续跑改用 `purifyDoneBatches` 集合而非计数）、**去掉 `reason` 字段**（输出格式改 `fragments: string[]`，解析仍兼容旧对象写法）、**漏答卡片单独小批重问一次**、**解析失败换温度重试一次**、逐批诊断日志（输入卡片/字数、返回条目、保留卡片/字数、整卡丢弃、漏答、原样保留、校验失败片段、句读吸附处数、重试次数）、`purifyScan.passthroughCards` 透出并在生成汇总显示「其中 N 张未获提纯结果、已按原样保留」。④ **修正阶段**——ts 补齐范围扩为「无时间戳**或时间缺少年份**」，提示词要求时间标注必须含年份（依据上下文/来源年鉴年份推断）且不得编造；本地 `hasYear`/`shouldFillTs` 只采纳含 4 位年份的结果（不含年份计入 `tsRejected` 诊断），已有含年份的 ts 不覆盖、缺年份的旧值允许被覆盖；残缺判定补充「句子起点/终点不完整」「缺少主谓宾」「指代不明」，并明确「**优先补全，不要删**」。新增/改写内联单测：提纯 11 项（含句读吸附、resolve 流程、提示词口径断言、去 reason 的解析断言、默认批次）+ 修正 2 项（缺年份识别与采纳规则、提示词要求）。验证：typecheck 零错误、**228 项单测通过（总计 229，1 项 watcher chokidar 环境失败为既有问题）**、生产构建成功。**待用户用真实 Provider 复跑同类任务复核：相关字占比目标 ≥65%、最终字数目标 ≤30,000、卡片边界不合格 0、缺年份 ts 0、提纯全部批次跑完且墙钟 ≤200s。**
> **2026-09-08（资料卡片「提纯」阶段：细读 → 提纯 → 修正 → 矛盾）**：用户实测反馈——切片策略「尽可能把一整段作为一张卡片」，导致大量卡片只有一两句与主题相关。我核对了本地库里「高中教育3」的真实数据：**191 张卡 / 48,063 字，逐句判定后相关句仅约 14,914 字（31%）、无关约 33,149 字（69%）；51 张卡（27%）通篇无一句相关**（无关内容主要是《长乐年鉴》的民生支出、老区扶贫、计生协、学校建设项目〔幼儿园/小学为主〕、教师聘任名单等）；根因是 `CHUNK_PARAGRAPH_MAX=1000` 整段成卡 + 细读提示词要求「相关时直接将其完整原文作为一张卡片」。新增**提纯**阶段并置于**修正之前**（用户选定 B 顺序）：① 大模型对每张整段卡片做**逐字摘录**，只保留与主题相关句段，输出 `{"items":[{cardId,fragments:[{text,reason}]}]}`；② **严格子串 + 本地校验**（`locateSpans` 按「去空白归一化」匹配后**回取原文真实子串**，容忍 `普 通 中 学` 类排版空格但不接受任何改写；校验失败片段丢弃、整卡失败保留原卡）；③ **保守提示词、代码不拦截**（「只要有可能的联系就保留，只有确定毫无关系才删」，列举可保留依据，不列举排除类别）；④ 本地 `coalesceSpans` 合并间隔 ≤24 字的相邻片段、单卡 ≤8 段（超出合并为一段，不丢内容）；失败/超预算（600s）时未提纯卡片**按原样保留**并透出 `purifyScan:{ok:false}`，纳入断点续跑（`phase: window|purify|repair|contradiction`）。**黑箱定位**（用户选定）：不加列/不加表、不展示提纯前原文、不支持单独回退（退回用「重新生成汇编」）、被舍弃内容不入回收站。窗口级矛盾说法经 `mapWindowGroupsThroughPurify` 映射到片段（否则落库按 excerpt 匹配不到卡片会整组丢失）；中断时落库的部分卡片也改用最新形态（提纯后/修正后）。离线估算：48,063 字 → 约 16,197 字（34%），卡片 191 → 166~214 张，44~51 张整卡丢弃；因修正/矛盾扫描输入随之缩小，**净增成本仅一趟提纯 pass**。新增 `src/main/writing/purify-service.ts`（7 项内联单测）+ `tests/compilation-service.test.ts` 提纯映射用例。验证：typecheck 零错误、222 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。**待用户用真实 Provider 复跑「高中教育3」复核：相关字占比应显著提升、片段可回原文定位、无改写无幻觉。**
> **2026-09-08（预设提示词 UI 重做 + 新增「输入标题与需求」）**：用户反馈该功能「风格过于简陋、交互与软件整体不符」——根因是 `.chat-panel__presets / __preset-btn / __preset-menu` **在 main.css 中根本没有任何样式**（原生 button + 无样式 div）。重做为与全站一致的商业风格：胶囊触发器（火花图标 + 文案 + 上向箭头，展开时箭头翻转、accent 高亮）→ **向上弹出的面板**（`var(--shadow-lg)` + 复用 `menu-pop` 动效、点击面板外/Esc 关闭、`min(430px, 100% - 24px)` 宽、内部滚动）；面板含标题 + 操作提示，并分两组：**「生成汇编前」**（新增预设 **「输入标题与需求」**：`本次撰写任务的标题为 「……」，具体包括 「……」`）与**「调整现有汇编」**（批量删除 / 增补内容，沿用原模板文本）。每项为一行卡片：图标 chip（内联描边 SVG，随主题色）+ 名称 + 说明 + 模板预览（两行截断）+ hover 出现的「填入」徽标；**无资料汇编时「调整现有汇编」置灰并标注「生成汇编后可用」**（因此 `ChatPanel` 新增 `hasCompilation` prop，`WritingWorkspace` 改为 `showPresetButton`（始终显示）+ `hasCompilation={hasComp}`，使「输入标题与需求」在首次生成前即可用）。交互改进：点击预设把模板**追加**到输入框（不再覆盖用户已输入内容）并自动**选中第一个「……」占位符** + 聚焦，用户可直接键入覆盖。文案全部进 i18n（`presetMenuTitle/Hint`、`presetGroupGenerate/Adjust`、`presetLockedHint`、`presetInsert`、`presetTitleReq*`、`presetBatchDeleteDesc`、`presetAddContentDesc` 等）。验证：typecheck 零错误、214 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。**视觉与交互手感待用户实测。**
> **2026-09-08（矛盾界面新增「定位到该段」）**：第一步矛盾面板中，每个说法在「采纳该说法」左侧新增「定位到该段」按钮——点击后下方资料卡片列表 `scrollIntoView({block:'center'})` 滚动到该说法对应的卡片并**短暂高亮**（`.compilation-card.is-located`，accent 描边 + 1.8s 脉冲动画），用户可就地编辑/删除/打开来源；实现为 `CompilationStep` 内部（`cardsRef` 容器 + `data-card-id` 锚点 + `locatedId` 状态），卡片已不在当前汇编（被删除或已随取舍排除）时显示明确提示（`locateMissing`）而非静默无反应。纯前端改动，无数据/契约变化。验证：typecheck 零错误、214 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
> **2026-09-08（资料卡片「大模型修正」：默认应用 + 卡片标记 + 可回退，并前移到矛盾检测之前）**：按用户需求变更重构原「二次加工/语义补全」。① **默认应用**——不再逐条「采纳/不用」，模型修正结果直接写入卡片；卡片 meta 行显示可点击标记「✎ 经过大模型修正」（回退后转灰「↺ 已回退到修正前」），点开弹窗看**修正前原文 / 修正后文本 / 修正理由**，并可按状态「回退到修正前」或「重新应用修正」（IPC `compilation:repairs:revert`/`repairs:apply`，均登记撤销栈）。② **前移到矛盾检测之前**——原为「生成汇编完成后再单独扫一次」，现作为生成管线内阶段串行执行：`窗口细读 → runRepairPhase → 卡片矛盾扫描`，使提交给矛盾检测的卡片内容更清晰完整；`CompilationResumeState.phase` 扩为 `window|repair|contradiction`，异常中断由「尝试继续」续跑（只重跑未完成批次，已完成批次结果保留）。③ **回收站降为两类**——不再收录修正条目（`compilation_repair_recycle_bin` 随 Migration 029 删除），弹窗只展示「资料卡片 / 矛盾」；被删卡片仍把其修正记录快照进 `compilation_card_recycle_bin.extra` 随卡片恢复。④ **时间戳自动补齐不算修正**（静默补 `ts`、无标记、不可回退）——但随管线前移后，落库按时间排序自然正确，不再需要事后 `reorderCompilationItemsByTs`。关键技术点：修正记录与卡片**同事务**写入（`CompilationItemInput.repair` 随卡片流经来源过滤与按时间排序，杜绝标记错位）、分批（30 张 / 12000 字，`splitRepairBatches`）串行 + 单批 300s 超时 + 整阶段 900s 预算（超预算标为未完成并在生成汇总提示）、上下文改用**同来源相邻段落**（卡片本身即整段，旧的「来源全文 ±120 字」窗口等价于无上下文）、窗口级矛盾的 variant 文本同步改写（`remapRepairedVariantExcerpts`，否则落库按 excerpt 匹配不到卡片会整组丢失）、`insertCompilationItems` 内写入 `compilation_repairs`（status='applied'）。老数据迁移：accepted→applied 保留；pending→applied 且把修订文本写入卡片（默认采纳口径）；rejected 丢弃。**术语**：代码/表名沿用 `repair`，界面文案称「大模型修正」。验证：typecheck 零错误、214 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功；待用户实测真实 Provider 下的修正质量与回退。
> **2026-08-28（资料库直接删除来源触发级联清理资料汇编，含批量删除）**：此前「删除资料库中的资料」直接删库，即使该来源已被资料汇编引用也不会清理卡片；只有「工作区文件被删除」才触发级联确认框。修复：`sources:delete` 与 `sources:deleteMany` 均先判 `isSourceUsedInCompilation`，若被汇编引用则先移入系统回收站并登记 `registerSourceRemoval(..., 'manual')`（`pendingCascade:true`，暂不删库），渲染层复用同一来源移除确认框（`origin:'manual'` 文案区分）决定「删除卡片/保留卡片」，再经 `workspace:sourceRemoval:decide` 删除来源（+可选卡片）；批量删除会逐个来源弹确认框。
> **二次修改彻底清理**：`deleteCompilationItemsForSourceIds`/`deleteCompilationItemsByIds` 除卡片/矛盾分组（含矛盾回收站，随 FK 级联）与 live 修订（`compilation_repairs` 随 item_id 级联）外，还显式清理 `compilation_repair_recycle_bin`（其 item_id 无外键，否则删除卡片后会残留指向已删卡片的“待恢复”修订）。`WorkspaceSourceRemovalPending` 新增 `origin:'workspace'|'manual'`。
> **去重（2026-08-28）**：同一来源会被“资料库手动删除 + 工作区文件移入回收站触发的对账”等多条路径重复登记，导致同一来源连续弹多次确认框；修复为主进程 `registerSourceRemoval` 幂等（已 pending 则不再通知）、增/全量对账 `reconcilePaths`/`reconcileWorkspace` 跳过已 pending 来源、渲染层 `enqueueSourceRemoval` 按 sourceId 去重，且**当前正在展示的确认项不放入队列**（否则确认后 `shift` 会把同一个确认框重新弹出来——已通过导出日志定位到该根因，主进程实际只登记一次，重复弹框纯属渲染层队列把当前项 shift 回来）。验证：typecheck 零错误、177 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建通过。
> **撰写工作台常驻挂载导致的“删除后卡片仍显示/来源空白”修复（2026-08-28）**：WritingWorkspace 切换页面仅隐藏不卸载，删除来源后其本地 compilation 状态不刷新，已删卡片仍以“来源空白”形式残留；新增 `reloadKey` prop，App 在来源移除确认完毕（delete/keep）后递增并传给 WritingWorkspace，触发重新加载，删除的来源卡片随即从界面消失。
> **二次修改自动补齐时间戳（2026-08-28）**：最近一次汇编中有大量卡片（如「首占璟月校区配套幼儿园二装工程」）无时间戳；`repair-service` 扫描提示词新增“对缺少时间戳的卡片结合原文上下文推断年份并给出 ts 字段”，解析输出新增可选 `ts`，扫描时对**缺失 ts 的卡片直接自动修改**（`updateCompilationItem`，无需用户采纳），语义补全/修订仍保留让用户采纳/不用；新增 2 项单测。
> **任务名未自动刷新 + 卡片时间排序修复（2026-08-28）**：① 任务名“新建任务”其实已在生成时被主进程改名（DB 已更新），但撰写工作台常驻挂载、任务列表未刷新故界面仍显示旧名；在 `handleGenerateCompilation` 成功分支补 `onChanged()` 刷新任务列表。② 汇编卡片在生成时按 ts 排序，但“缺失时间戳→排最后”的卡片被二次修改自动补齐 ts 后**未重新排序**，导致末尾大量时间倒挂（如 2024 之后接 2015/2017）；新增 `reorderCompilationItemsByTs`（无 ts 排最后、按年份升序重写 position），在二次修改补齐时间戳后重排整份汇编；新增 1 项单测。
> **资料汇编“按时间排序/切换正反序”按钮（2026-08-28）**：在「确认汇编，进入下一步」按钮左侧新增「按时间排序」按钮，点击即按时间重排汇编（正序=旧→新，再次点击切为反序=新→旧，循环切换）；新增主进程 IPC `compilation:reorder`（`reorderCompilationItemsByTs` 支持 direction）、preload `reorderCompilation`，`CompilationStep` 新增按钮与 `sortOrder` 状态，`WritingWorkspace` 新增 `handleReorderItems` 并刷新汇编。
> **按时间排序圆钮 + 撤销/恢复（2026-08-28）**：① 排序按钮改为圆形按钮（圆钮），图案为 ↑/↓ 体现正/反序切换状态；② 在其左侧新增「撤销」「恢复」两个同风格圆钮；③ 撤销/恢复采用“快照 + 栈”机制（`writing/compilation-undo.ts`），对每次汇编可变操作（LLM 调整、手动编辑卡片、删除卡片、矛盾采纳/忽略、二次修改采纳/不用、回收站恢复矛盾/修订、排序、确认等）登记撤销栈，撤销=恢复上一个完整快照、恢复=重做下一个快照；新增 IPC `compilation:undo/redo/undoState` + preload，`WritingWorkspace` 新增 `handleUndo/handleRedo` 并在汇编变化后同步撤销/恢复步数，`CompilationStep` 新增三个圆钮；新增 1 项撤销/恢复单测。
> **回收站新增「资料卡片」第三类（2026-08-28）**：被删除的资料卡片（单卡删除、以及汇编调整批量删除）会快照进新增的 `compilation_card_recycle_bin`（Migration 023，含卡片行 + 其矛盾变异/语义补全修订 JSON），回收站现在含**资料卡片/二次修改/矛盾**三类，统一按删除时间倒序（最新删除最先显示，栈式排序）；恢复卡片会连同其矛盾变异/语义补全修订一起还原；来源级联清理仍为硬删除不入回收站（来源已删，恢复外键悬空）。`CompilationRecycleBinItem` 新增 `kind:'card'`，IPC restore 响应新增 `card`。新增 1 项单测。
> **网页资料库行为优化（A1/A3/B4/C6，2026-08-28）**：① A1 站点发现改为 **sitemap 优先**（递归 sitemap index，`parseSiteMap`；无 sitemap 才回退 BFS）；② A3 URL 规范化 + 去重（`normalizeArticleUrl`/`dedupeArticleKey`：小写主机、去默认端口、去跟踪参数/尾斜杠/fragment）；③ B4 条件请求（`fetchUrl` 支持 If-None-Match/If-Modified-Since，304 复用已有正文；`web_site_articles` 新增 etag/last_modified/body_hash/last_fetched_at，Migration 024）；④ C6 礼貌限速（`parseRobotsTxt`/`fetchRobotsTxt`/`isPathDisallowed` + 站点级串行延迟，discover/import 均遵守）。
> **A2 RSS/Atom 订阅源（2026-08-28）**：`parseFeed`/`detectFeedUrls` + `fetchFeedArticles` 优先解析 RSS2/Atom（首页 `<link rel=alternate type=application/rss|atom+xml>` + 常见 feed 路径），站点发现顺序改为 **feed → sitemap → BFS**；无文章标题者保守保留为候选，导入正文时用页面 `<title>` 补齐。
> **诊断日志增强**：`[web]` 类别打印站点发现方式（feed/sitemap/bfs）+ 文章数、网页资料检索每站点“文章清单/标题命中/落库/robots crawl-delay”、单篇“304 复用/抓取落库/正文精过滤丢弃”。新增纯函数单测。
> **D8/E10/E11 已完成（2026-09-01）**：D8 新增 extractArticleText（article/main/正文容器优先、保留表格行列、去 script/style/nav/footer，正文过短回退 stripHtml，诊断日志标注提取器）；E10 新增 extractPublishedDate（meta published_time/publishdate/pubdate/date + <time datetime> + 可见日期文本），web_site_articles 新增 published_at（Migration 025），文章清单按 COALESCE(published_at, discovered_at) DESC 排序；E11 曾落地「用户按站点配置关键词」，已于 2026-09-01 应产品要求**移除**（Migration 026 删列、IPC/preload/UI/召回逻辑一并删除）。验证：typecheck 零错误、189 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建通过。

> 完整的历史修改日志已整理进 `PLAN.md` 各阶段摘要；此处保留对未来开发仍有价值的根因结论。

- **（修复：长任务“保持唤醒”改为真正不熄屏，并覆盖全过程，2026-09）**：原先用 `prevent-app-suspension`——**只防系统睡眠、屏幕仍会自动熄灭**，所以“AI 细读时不熄屏”其实并不成立。改为 **`prevent-display-sleep`**（既阻止显示器休眠，也保持系统不休眠）；并把长任务覆盖范围对齐“进度条全过程”：`compilation:generate`（AI 细读 + 卡片矛盾检索与汇总）、`compilation:continue`（中断续跑）、`compilation:repairScan`（二次修改/语义补全扫描）、`compilation:adjust`（汇编调整，新增包裹）、`writing:generateDraft`/`draft:regenerate`、`sources:summarizeAll` 共 7 处均以 `withKeepAwake` 包裹（引用计数、幂等，任务结束/异常在 finally 释放）。设置项文案同步为「长任务时保持电脑唤醒（不熄屏）」。验证：typecheck 零错误、204 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（重构：卡片矛盾扫描改为“本地预筛 + 少次调用 + 时间预算”，2026-09）**：用 `llm_call_logs` 定位到根因——**单次扫描耗时主要来自推理本身、与输入量弱相关**（3.4k 输入也要 34~132s；20k≈214s；54k 超时），因此阶段耗时 ≈ 调用次数 × 1~3 分钟，旧实现“小批次 × 双温度”导致 7~10 分钟（比细读还久），且全程只发一条“正在汇总卡片间的矛盾…”看起来卡死。改造：① **本地预筛（阻断法）**`clusterCandidateCards`——按 bigram 相似度（去空白/标点）并查集聚类，只保留 ≥2 张的候选簇，孤立卡片直接跳过；② **少次调用**`packCandidateCalls`——同簇不拆、按字符预算 15k/数量 100 打包，通常 **1~2 次调用**；③ 扫描输入**压缩**（每条只给前 150 字）+ 提示词明确“只比同一事实、无冲突输出空数组、不解释”；④ `maxRetries: 0`；⑤ **整阶段时间预算 360s**，超时即停止并把 `contradictionScan.ok=false` + 原因透出（不再无限等待）；⑥ **分批进度**（percent 88→99、`正在检索卡片矛盾（第 i/N 批）`）。真实数据验证：99 张卡 → 14 个候选簇/51 张候选卡（48 张孤立卡跳过）→ **1 次调用（约 9.4k 字符）**。验证：typecheck 零错误、204 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（修复：卡片矛盾扫描仍超时——进一步缩小单批、单温度、串行，2026-09）**：矛盾扫描继续超时。进一步：① `CARD_SCAN_MAX` 60→**25**、`CARD_SCAN_CHARS` 6000→**3000**；② 去掉双温度（`[0,0.3]`→单次 `0`），`scanCardContradictions` 改单次调用（失败即失败、可续跑重扫）；③ `runContradictionPhase` 由按 Provider 并发 `Promise.all` 并行改为**串行一次一批**（不再并行，避免并发放大超时/限流）。验证：typecheck 零错误、202 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（优化：剩余时间预估更稳更准 + 卡片保留整段上下文，2026-09）**：① **ETA（A+C）**——窗口细读改为“按字符加权”：用每字符秒数（`medianNumber` 取最近 5 窗中位数）× 剩余字符数外推，`WINDOW_ETA_ALPHA` 0.7→0.35、新增预热期（<2 窗用窗口数×默认先验）；矛盾扫描先验按“预计卡片批数×批均耗时”粗估，不再固定 30s。② **卡片上下文（方案 C 修正）**——`chunkByParagraphs` 改“混合合并”：`【/一、/◆/■/数字+、` 条目行内把多句/多行合并为一条（保留主语/上文，避免“其中预科班…”缺上下文的残句），非【文本仍按句末标点断段（保住“保守本地闸门”按段剔除无关内容）；细读提示词改为“每条候选=完整条目/整段，相关则直接整段成卡，仅极长条目才按事实拆成自包含卡片”。真实年鉴验证：`【华侨中学新疆高中班】…` 条目保持整段完整、无孤立“其中预科班”残句，逻辑段 29412 物理行→4089 段。验证：typecheck 零错误、202 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（修复：卡片矛盾扫描频繁超时/中断，2026-09）**：方案 C 后资料卡片变为整段/较长事实，单批 200 张的“卡片矛盾扫描”输入过大，模型逐两两比对耗时超出 `CARD_SCAN_TIMEOUT_MS`（300s）触发“模型响应超时”，进度中断。修复：① `CARD_SCAN_MAX` 200→**60**、新增 `CARD_SCAN_CHARS=6000` 按“卡片数+累计字符”双预算切批（`splitCardScans`），`runContradictionPhase` 改用该批打；② `CARD_SCAN_TIMEOUT_MS` 300s→**420s**；③ `scanCardContradictions` 改为**任一温度解析成功即视为本批成功**（防止单个温度超时把整批误判为中断；两温度都失败才降为失败）。新增 `splitCardScans` 单测。验证：typecheck 零错误、201 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（回退方案 A，改走方案 C：让大模型在细读时重建段落，2026-09）**：方案 A（基于 PDF 坐标/行距的几何段落重建）在“高中教育2”仍残留句中切断/表格索引行/换行符等硬伤，几何启发式难以彻底克服，已回退到 `44947fa`。改取 **C1 + C2**：C1 恢复“模型重建段落 + 合并断句 + 跳过噪声”的细读提示词（excerpt 必须完整句子/事实、不得句中截断、遇到页码/目录/索引等噪声直接跳过、多条候选为同句延续则合并）；C2 在 `chunkByParagraphs` 内做纯文本级“按句合并被换行打断的物理行 + 过滤页标记/纯页码/目录点线”（索引/数据行不滤，交给模型）；无需 PDF 坐标，Word 等其它格式不受影响。真实年鉴验证：29412 个物理行 → 2805 个逻辑段，`【概况】…重点名校 17 人。`完整还原，无句中切断残留（仅剩 1 条真实目录索引行）。验证：typecheck 零错误、200 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（撰写拆分为「生成汇编 / 撰写初稿」两个功能区，2026-09-07）**：取缔三步导航条，把「撰写」拆成两个独立功能区——侧边栏改为「资料库 / 生成汇编 / 撰写初稿 / 设置」。`writing_tasks` 新增 `mode` 列（`compile`/`draft`，Migration 028，升级时清空本地旧任务）；两个功能区任务列表完全独立，各自由「新建任务」按类型创建。**生成汇编**保留原第一步（生成/调整/矛盾取舍/语义补全/排序/撤销/回收站），确认按钮改为「导出资料汇编」——先 `finalized` 再弹两种格式：`.docx`（jszip 手工生成最小 OOXML）或软件专用 `.xzsc`（JSON，含汇编/卡片/矛盾/语义补全）；新增 IPC `compilation:exportDocx/exportArchive`。**撰写初稿**保留第二、三步：新建任务后必须导入资料汇编——可从「生成汇编」已完成任务任选（深拷贝到本任务，`importCompilationIntoTask`）或导入外部 `.xzsc`（先预留按钮 + `compilation:importArchive` 占位 `NOT_IMPLEMENTED`）；导入后先显示「指定行文规范」面板，点「开始撰写」进入初稿编辑器（`DraftEditor`）。演示任务改为「生成汇编」「撰写初稿」各一份；新手教程步骤/文案同步。验证：typecheck 零错误、198 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（整段化切片 + 强制三步式，Phase A/B）**：① **整段化切片**——新增 `chunkByParagraphs`（默认上限 `CHUNK_PARAGRAPH_MAX=1000`，超长段按句折成 ≤上限 子块并共存同一 `paragraphIndex`），粗细筛改为**整段级保留/剔除**（段内任一子块有信号 → 整段所有子块保留）；**资料卡片=整段/整子块**（AI 细读提示词改为“每条候选材料直接作为一张卡片，excerpt=整段原文，不再按事实切分”，旧“AI 切事实卡”暂移除、后续可补回）。② **强制三步式/移除旧链路**——`generateDraft`/`regenerateDraft` 的 `compilationId` 改为**必填**且校验汇编 `finalized`，缺失/未确认返回新错误码 `COMPILATION_NOT_FINALIZED`；删除“无汇编时的旧检索生成”入口（前端拦截、不再调用）；保留 `retrieveChunks`/`chunkText`（向量索引、文段来源询问、检索预览仍用）。旧检索编排代码暂留在 `generate.ts` 中但已不可达（后续可单独清理）。确认：`WritingGenerateDraftReq`/`DraftRegenerateReq.compilationId` 必填、preload 签名同步。验证：typecheck 零错误、198 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（429 限流断点自动续传 Phase A/B）**：区分限流（可重试）与余额不足/其他（不自动重试）；`readWindow`/`scanCardContradictions` 以 `result.error?.code===LLM_RATE_LIMIT` 识别 429 并标记 `rateLimited`/`interrupted.retryable=true`；生成管线在限流中断时自动降本次并发数（`reduceConcurrency` 减半、最小 1，**不写回 Provider 设置**，仅本次生成生效）、退避后从断点自动续跑（`runWithRateLimitAutoResume`，上限 2 次、间隔 10s/25s）；降并发时经新事件 **`compilation:advice`（`{taskId, kind:'reduce-concurrency'}`）** 推送建议，渲染层翻译为「建议降低当前大模型的并发数」并持久化到对话；若仍限流，前端自动续传兜底（最多 2 次、间隔递增，`AUTO_RESUME_LIMIT`）。`CompilationInterrupt` 增加 `retryable?`。验证：typecheck 零错误、196 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（长任务防睡眠 + 按 Provider 并发处理窗口，Phase A/B）**：**A 防睡眠**——主进程新增 `power/keep-awake.ts`（封装 electron `powerSaveBlocker`，`prevent-app-suspension` 保持系统活跃且屏幕可关），引用计数/幂等、无 electron 降级 no-op；在 `compilation:generate/continue/repairScan`、`writing:generateDraft/regenerate`、`sources:summarizeAll` 的长任务 handler 用 `withKeepAwake` 包裹（入口 start、finally stop）；设置页总览新增「长任务时保持电脑唤醒」开关（`AppSettings.keepAwake`，默认开启）。**B 并发窗口**——每个 Provider 可配置「并发窗口数」（`llm_providers.concurrency`，Migration 027，默认 4、范围 1–8）；设置页 Provider 表单新增「并发窗口数」输入；`compilation-service` 的 `WINDOW_CONCURRENCY` 由常量改为读取 Provider 配置，窗口细读并行数=该值，且矛盾扫描按「波次」并行（每波 ≤ concurrency 批；任一波失败以该波起点中断，续跑重扫该波，与断点续传兼容）。文档同步。验证：typecheck 零错误、195 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（前端界面调整：顶栏移除 + 中栏显隐改为边界手柄 + 资料库中栏分区）**：① 移除最上方横栏（软件名「志书撰写工具」/「隐藏中栏」按钮/版本号/平台信息）；② 「隐藏中栏」功能改为**中栏/右栏边界悬停切换手柄**——光标放到边界时边界线高亮并出现圆角长方形小三角按钮（中栏可见=左向、隐藏=右向），点击切换中栏显隐（持久化），并保留拖动边界调宽；③ 资料库中栏顶部保留「导入」「整理资料」按钮（去掉「全部资料」标题），下方分「网页资料库」「本地资料库」两段小标题；标签管理/资料管理入口由顶部「…」菜单并入工具栏菜单。新增 `PaneEdgeToggle` 组件、`sourceList.webTitle/localTitle`、`paneEdge.*` 文案。验证：typecheck 零错误、192 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **（资料汇编生成异常中断断点续传，Phase 6.x 新增）**：用户试用反馈——生成资料汇编时若大模型异常中断（余额不足/网络），进度条卡住且无反馈，修复后只能重开任务。新增**会话内断点续传**：① `compilation-service` 把窗口细读与卡片矛盾扫描改为可中断/可续跑（`resumeStore` 记录 phase/doneSet/scanOffset/windowOutputs，`readWindow` 区分「LLM 异常 → 中断」与「无解析输出 → 正常」）；② 生成开始时即创建 `drafting` 汇编，中断时把已完成窗口卡片落库（`replaceCompilationItems`），前端可看到部分卡片；③ 新增 IPC `compilation:continue` + preload + main handler，`continueCompilation` 从断点继续（复用已完成输出，仅重读失败/未完成窗口、续扫剩余矛盾批次），再次异常可再「尝试继续」；④ 渲染层 `CompilationStep`/`ChatPanel` 在左栏进度区展示中断提示 + 「尝试继续」按钮，中断时不运行语义补全扫描。文档与 `docs/shared-contracts.md`/`ui-architecture.md` 同步。验证：typecheck 零错误、新增 2 项断点续传单测、生产构建成功。
- **2026-09-06（Phase 6.7 测试/文档/发布完成 + Phase 5 重命名为 Last Phase）**：完成 Phase 6.7 的文档同步与全量验证——`docs/data-model.md`/`shared-contracts.md`/`ui-architecture.md` 更新至当前三段式代码现状（资料汇编/二次加工/三类回收站/规范文档库/参考范本/按步骤默认模型/网页资料库优化/PDF cmaps/settings 键）；`README.md` 功能与技术实现更新（三步向导/主题切换/按步骤默认模型/网页资料库/PDF cmaps）；`PLAN.md` 标记 Phase 6.7 完成，并将「Phase 5: Acceptance & Packaging」重命名为 **Last Phase（收尾阶段）**——明确其为整个项目收尾阶段、在执行顺序上位于全部功能阶段之后，避免与其它阶段的序号顺序混淆；`AGENTS.md` 同步当前状态（Migration 001–026、验证基线 v0.2.1：typecheck 零错误、190 项单测 + 1 项 watcher chokidar 环境失败、生产构建成功）与近期记录。验证：typecheck 零错误、190 项单测通过（1 项 chokidar 环境失败为既有问题）、生产构建成功。
- **2026-09-01（网页资料库：修复「打开来源跳到栏目列表页」）**：`clnews.com.cn/html/22/list.shtml` 这类列表/栏目页因扩展名 `.shtml` 被 `isArticleUrl` 误判为文章页，sitemap 发现时以空标题收录，抓取后正文提取器把列表页内容当作正文、`matchesExact` 命中其中某条文章标题/摘要，于是列表页被当成单篇来源落库。修复：新增 `isListPageUrl`（按 basename 名单 `list/index/default/channel/category/column/col/lm/more/news_list` 判断，真实文章 basename 为日期/文章 ID/数字字母串，不受影响），① `isArticleUrl` 先排除列表页（发现阶段不收录），② `importSiteArticle` 入口再兜底（抓取/复用阶段直接丢弃并记日志）。新增单测。验证：typecheck 零错误、190 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建通过。
- **2026-09-01（资料库精简 & PDF 中文乱码修复）**：① 移除「站点关键词（E11）」功能（Migration 026 删 web_sites.keywords 列；IPC/preload/UI/site-crawler 召回逻辑一并删除）；② 移除「复制全文」按钮（SourceViewer）；③ 移除网页资料库「同步」手动按钮+IPC/preload（生成汇编时仍自动 `syncSite`，不影响抓取）。④ 修复中文/CID 字体 PDF 文字提取与预览空白：根因是 pdf.js 未提供 cMapUrl/cMapPacked —— 主进程 `parsePdf` 注入 cmaps 目录（`setPdfCmapsDir`）；文件服务器新增 `/pdf-cmaps/<name>.bcmap` 路由 + IPC `app:getPdfCmapsUrl`，`PdfViewer` 传给 `getDocument({ cMapUrl, cMapPacked:true })`。验证：typecheck 零错误、190 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建通过。
- **2026-09-01（资料汇编矛盾扫描 & 二次修改可靠性）**：① 矛盾扫描曾对 `deepseek-v4-flash` 在 120s 内超时（`LLM_TIMEOUT`）且错误被静默吞掉 → 显示“0 矛盾”；现`CARD_SCAN_TIMEOUT_MS` 120s→300s、按 `CARD_SCAN_MAX` 分批扫描覆盖全部卡片、任一批失败即标记并透出 `contradictionScan:{ok,message}`，前端在生成汇总里提醒“矛盾扫描未完成，可能存在遗漏”。② 二次修改（`compilation-repair-scan`）此前在某任务未留下任何调用痕迹且 `compilation_repairs` 为空（137 张缺 ts 未补）；respair-scan 主 handler 对 `pushUndo` 加 try/catch 防阻断、repair-service 增“开始扫描/失败/抛错”日志、前端 `scanRepairsAndReload` 捕获失败时记 `[repair]` 日志。验证：typecheck 零错误、190 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建通过。
- **2026-09-01（资料汇编「预计还需」优化 A+C）**：把第一步生成资料汇编的剩余时间从「固定 20 秒/窗口」常数改为**实测均速外推**（A）——窗口细读阶段记录每个窗口真实耗时、EMA（α=0.7）校正每窗均速，剩余 = 均速×剩余窗口 + 矛盾汇总先验；前置各单次阶段（提取关键词/网页检索/召回/保守闸门/矛盾汇总）改用阶段先验预算，窗口块未算出前用占位预算避免前置阶段预估明显失真。前端（ChatPanel）对剩余秒数做 EMA 平滑并**区间化显示**（约 X–Y 秒/分钟），避免数字抖动、诚实体现 LLM 耗时不确定性。验证：typecheck 零错误、189 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建通过。
- **2026-09-06（发布 v0.2.1 并包装安装包）**：语义化版本号 0.2.0 -> 0.2.1；`npm run dist:win` 生成 `dist/XieZhiShu-Setup-0.2.1-x64.exe`（252MB）+ `.blockmap`，作为资产上传到 GitHub Release v0.2.1（tag `v0.2.1`，附发布说明）。内容涵盖：网页资料库（feed/sitemap/BFS 发现、D8 正文提取、E10 发布时间、列表页修复、移除站点关键词/复制全文/同步、新增站点「修改」）、PDF 中文/CID 字体修复（cmaps）、资料汇编可靠性（矛盾扫描超时放宽+分批+失败提示、二次修改、预计剩余时间 A+C）。验证：typecheck 零错误、190 项单测通过（1 项 watcher chokidar 环境失败为既有问题）、生产构建成功。
- **2026-08-28（发布 v0.2.0 并固化发布规则）**：语义化版本号 0.1.4 -> 0.2.0；首次按新规则发布——`npm run dist:win` 打包 Windows NSIS 安装包（`XieZhiShu-Setup-0.2.0-x64.exe` + `.blockmap`），并作为资产上传到 GitHub Release v0.2.0（用户要求：后续每个版本 Release 都需包含 win 安装包）。electron-builder 默认会触发 `@electron/rebuild` 源码重编译 better-sqlite3（需要 Visual Studio），但 better-sqlite3@13 使用 N-API 预编译产物（`prebuilds/win32-x64.node`，Node/Electron 通用），故在 `build` 配置加 `npmRebuild: false` 跳过重编译，产物可直接打包运行。
- **2026-08-28（重写新手教程 + 演示任务 + 规范点击跳转修复）**：① **新手教程重写**——OnboardingOverlay 聚光引导步骤重构为 6 步：配置大模型（手动 Provider）/ 预设大模型参数 / 设置工作区 / 了解资料库 / 演示任务 / 三步生成初稿；onboarding-copy.ts 重写，并新增 data-onboarding 锚点 settings-preset、sources-library、writing-demo-task、writing-stepper。② **演示任务**——新增 src/shared/demo.ts（DEMO_TASK_TITLE=测试任务（仅作为演示））与 src/main/db/demo-task.ts（ensureDemoTask() 幂等种子：对话历史 + 资料汇编 7 卡（1 组矛盾 pending + 1 条语义补全 pending）+ 已确认汇编 + 志书初稿；index.ts 启动时调用），并加 2 项单测；撰写页引导步骤切换时自动打开演示任务（handleOnboardingStepChange 选中 demo），使三步工作台/步骤条可见。③ **规范点击跳转修复**——StyleGuideEditor 的 useEffect([reload, mode]) 在 mode 变为 editor 时用默认规范覆盖 openGuide 选中的条目，导致点击任何条目都跳到同一篇规范；改为仅在 currentId === null 时自动载入默认规范。‍**引导定位修复**——设置页 Provider/预设/工作区等区块位于页面下方，未滚动时高亮框与说明卡片会落到视口外：useTargetRect 在定位目标时 scrollIntoView({block:'center'})，若目标不可见则先滚入；提示卡片改用实测宽高钳制（附 .onboarding-card__body 限高内部滚动）。引导支持单步多目标：第 1 步同时框选「LLM Provider 配置」与「步骤默认模型」两个模块，卡片按联合矩形定位。验证：typecheck 零错误、176 项单测、生产构建通过。
- **2026-08-25（深色主题对比度修复 + 跨任务切换进度条丢失修复）**：① **深色主题对比度**——表单控件（`.chat-panel__input`、`.writing-form__input/select`、`.settings__input`、`.draft-editor__heading`、`.source-list__url-input`、`.tag-manager__input`）此前未设 `color`，受浏览器 UA 默认黑色文字影响在深色下低对比；统一加 `color: var(--fg)`（部分补 `background: var(--bg)`）。② **跨任务切换进度条丢失**——根因：`WritingWorkspace` 以 `key={taskId}` 挂载，切到其他任务即整体卸载，进行中的 `busy/busyText/progress/streamText` 临时状态随之丢失；且生成是单个长调用、阶段间无事件，切回后无事件重建。修复：保留按任务 `key`（避免跨任务污染），新增**模块级 Map `transientByTask`**——组件卸载时把当前生成中状态存入 Map（`liveTransientRef` 每渲染同步），重新挂载时恢复，从而切走再切回进度条消息不消失。验证：typecheck 零错误、174 项单测、构建通过。
- **2026-08-25（资料汇编矛盾发现稳定性优化：B+C+D + seed 铺垫）**：针对「同样提示词+同样资料库，两任务矛盾发现不一致」——根因是 LLM 采样非确定性经「关键帧提取→候选集→窗口→卡片集→矛盾扫描」逐级放大。落地低成本项：**B** 关键帧提取按「撰写要求」缓存（`keywordExtractionCache`），同一指令复用同一套粗筛关键词（消除第一层漂移）；**C** `recallCandidateChunks`/`recallCompilationCandidates` 结果确定性稳定排序（by sourceId+position），窗口切分顺序稳定；**D** 卡片级矛盾扫描改为 0/0.3 两温度各扫一次后按主题并集去重（`CARD_SCAN_TEMPERATURES`，仅小卡片集，成本可忽略）；**seed** 铺垫——`ChatCallOptions.seed` 透传，关键帧提取/细读/矛盾扫描固定 `REPRODUCIBILITY_SEED`（Provider 支持则生效）。验证：typecheck 零错误、174 项单测、构建通过。
- **2026-08-25（资料卡片二次加工：语义补全/修订 + 回收站统一）**：在 Step-1 资料汇编后增加第二次 LLM 语义补全扫描——卡片表意不明/疑似残缺时读取来源原文上下文，生成 pending 修订（`compilation_repairs`，Migration 021），前端绿色边框 + 原/补全对比 + 原因 + 采纳/不用；采纳改写卡片摘录、拒绝不改写，均快照进 `compilation_repair_recycle_bin` 供恢复。回收站改名「回收站」并统一展示矛盾与语义补全条目。新增仓储 `db/compilation-repairs.ts`、服务 `writing/repair-service.ts`、IPC `compilation:repairScan/repairs:list/repairs:decide` + preload + 主进程 handler；`Compilation.repairs`、`CompilationRecycleBinItem` 改为判别联合（contradiction/repair）。验证：typecheck 零错误、174 项单测、构建通过。
- **2026-08-25（GLM-4-Flash 密钥解密崩溃修复：测试无反应/生成无回复）**：`getProviderSecret` 对 `codec.decrypt(row.api_key)` 未做兜底——若某 Provider（如智谱 GLM-4-Flash）已保存的加密密钥无法解密（存储被破坏/编码不匹配），`decrypt` 抛错并沿 `testProviderConnection`/`resolveTaskProvider` 一路上抛，导致 IPC promise 被 reject：前端 `handleTest` 的 `await` 抛错后既不落“测试中...”也不显示结果（看似无响应），生成侧 `generateDraft`/`regenerateDraft` 同样 reject，前端无错误提示（只有“已发送”无回复）——与「DeepSeek 能显示测试中/GLM 不能」的用户反馈一致。修复：① `getProviderSecret` 用 try/catch 包住 decrypt，失败返回 `null`（调用方按“Provider 不存在”降级，不再抛错）；② `testProviderConnection` 对 `getProviderSecret` 再加防御 try/catch；③ 前端 `handleTest` 与 `handleGenerateDraft`/`handleRegenerate` 均加 `catch`，任何 IPC reject 都会落到左侧/行内提示。验证：typecheck 零错误、166 项单测、构建通过。
- **2026-08-25（LLM 请求无限挂起修复：硬超时兜底 + 测试连接反馈）**：用户将第 3 步默认模型设为智谱 GLM-4-Flash 后，第三步生成初稿「已发送请求但一直无回复」、设置页点击「测试连接」无结果。根因：`net.fetch` 在目标主机（如大模型站点）未按预期响应 abort 时，`await net.fetch` 永不 settle → 生成（10 分钟超时）与测试（15 秒超时）都看起来无限挂起、无任何反馈。修复：`src/main/llm/chat.ts` 的 `requestOnce` 与 `src/main/llm/test.ts` 的 `testProviderConnection` 都改为 `Promise.race([net.fetch(...), hardTimeout])`——守护定时器既 `controller.abort()` 又 reject，保证到 timeoutMs 一定返回（超时/网络失败给出明确错误），后端生成也必然返回（前端已能在失败时 `appendAssistant`）。验证：typecheck 零错误、166 项单测、构建通过。
- **2026-08-25（重新生成初稿修复 + 大模型配置收敛到步骤默认）**：① **修复「重新生成初稿」左侧无反应**——`generateDraft`/`regenerateDraft` 在 Provider 解析失败（如未设步骤默认模型）时不会落库助理消息，前端 `handleGenerateDraft`/`handleRegenerate` 也静默忽略 `!res.ok`，导致对话框无任何反馈。已为初始生成与重新生成都补充显式错误提示（`!res.ok` 时 appendAssistant 错误）。② **确认重发走第 3 步默认模型**——Provider 解析改为**一律以步骤默认模型为准**（不再优先任务固定 provider、不再用全局「默认大模型」）：第 1 步用 `settings.compilationProviderId`、第 3 步/对话/来源询问用 `settings.draftProviderId`、摘要整理用 `settings.compilationProviderId`。③ **删去全局默认模型**——移除 `AppSettings.currentLlmProviderId` 及设置页「设为当前」按钮/「当前大模型」总览项/删除当前 Provider 时的清理逻辑；preload `updateSettings` patch 同步去掉该字段。④ **删去任务级大模型选择**——移除撰写工作台输入框上方的 Provider 下拉（ChatPanel `providers/providerId/onProviderChange` props 与 `handleProviderChange`、`providers` state 一并删除）。验证：typecheck 零错误、166 项单测、构建通过。
- **2026-08-25（新增 Qwen3-Max / GLM-4.6 预设 + API key 教程）**：在 `src/shared/llm-presets.ts` 内置预设新增两项——**通义千问 Qwen3-Max**（`qwen3-max`，apiBase `https://dashscope.aliyuncs.com/compatible-mode/v1`，阿里云百炼）与**智谱 GLM-4.6**（`glm-4.6`，apiBase `https://open.bigmodel.cn/api/paas/v4`，智谱开放平台），并各配「注册→实名/开通→创建 API Key→复制→填入软件」五步教程；单测更新为覆盖五套预设。验证：typecheck 零错误、166 项单测、构建通过。
- **2026-08-25（编辑器中栏微调 + Phase 6.8 按步骤默认大模型）**：① **编辑器中栏**——去掉初稿编辑器的固定 760px 最大阅读宽度，改为**固定左右留白（24px 28px）随右栏宽度实时铺开**，正文字号 14→16px 略微调大（`.draft-editor__doc .tiptap`）。② **按步骤默认大模型**——按用户澄清改为**设置界面全局**配置：`AppSettings` 新增 `compilationProviderId`（第 1 步资料汇编）/`draftProviderId`（第 3 步生成初稿），存 settings 表 key（无需迁移，保存/清空校验 Provider 存在）；设置页新增「**步骤默认模型**」区块（两个下拉，选项=已配置 Provider + 「未设置（回退任务/全局）」，中栏导航新增「步骤默认模型」）。Provider 解析 `resolveTaskProvider(task, step)` 优先级改为 `任务固定 → 步骤默认 → 全局当前`，`generateCompilation` 用 compilation 默认、`generateDraft` 用 draft 默认、对话维持现有回退。验证：typecheck 零错误、166 项单测、构建通过。
- **2026-08-25（Phase 6.5 三套主题 + Phase 6.6 初稿编辑器深改）**：① **主题**——设置页新增「外观（主题）」区块，三选一（简洁明亮 / 明亮+深色 / 古典公文风）；`App` 维护 theme 状态并以 `data-theme` 注入 `<html>`，`main.css` 用 `:root[data-theme=...]` 覆盖全部 CSS 变量（背景/面板/边框/文字/强调/危险/阴影等），localStorage `ui.theme` 持久化记忆。② **编辑器深改**——正文衬线（宋体/Noto Serif SC）、760px 最大阅读宽度、标题层级、页边距、`@media print` 打印友好；保留 Markdown 存储/800ms 防抖整稿保存/draft:updateContent/右键菜单/撤销重做工具栏/页脚字数统计。验证：typecheck 零错误、165 项单测、生产构建通过。
- **2026-08-25（第二步「保存规范」覆盖无反应修复 + 新增「下一步」入口）**：① **修复**——点击「保存规范」→ 选已有规范 → 二次确认「覆盖」后无反应：根因是 `doSave` 成功后未清空 `saveTarget`，覆盖确认框一直残留，用户看到的就是“无反应”。修复：成功后置 `setSaveTarget(null)` 关闭确认框，并给 `ConfirmDialog` 增加 `busyText`/`error`（覆盖按钮忙碌时显示「保存中」，失败时在框内显示错误原因）。② **新增「下一步」**——「保存规范」按钮右侧新增「下一步」按钮（仅第二步 taskId 场景显示）：若当前规范相对已保存内容有修改（`content !== savedContent`，配套记录 `currentId`/`savedContent`），点击弹二次确认「将使用上次保存的规范、未保存修改会被丢弃」，确认后进入第三步；否则调用 `onNext`——**进入第三步并自动触发 `handleGenerateDraft`**（取 `compilationInstruction` 或 `task.userInstruction` 作为撰写要求，按已确认汇编 kept 卡片+默认规范+可选范本组织提交物），生成进度与结果在左侧对话框体现；已有初稿时仅切换页面不重复生成。验证：typecheck 零错误、165 项单测、生产构建通过。
- **2026-08-25（第三步「生成撰写初稿」落地：三步提交物协同 + 移除底部导航按钮）**：① 移除工作区底部「上一步 / 下一步」按钮，切换仅靠顶部三步导航（`writing-stepper`，仍按未确认汇编锁定 Step 2/3）。② Step 3 提交物固定为三样——已确认资料汇编的 `kept` 卡片文本（矛盾取舍中被排除的卡片 `kept=0` 不纳入，按时间排序去重）、第二步确认的默认行文规范（`getDefaultStyleGuide`）、可选任务级参考范本（`task.modelText`）。③ 提示词组织：`buildUserPrompt`（新增 `materialsOrigin` 可选参数）按「用户要求 → 写作规范 → 参考范本（可选）→ 参考材料（标注来自已确认资料汇编、已剔除矛盾排除卡片）」注入，系统提示仍为默认规范 + JSON 输出契约；`generateDraft` 汇编分支读取三者并构建 messages。新增 2 项单测（参考范本注入、参考材料来源标注），共计 165 项全通过；typecheck/构建通过。
- **2026-08-25（Phase 6.4.2 第二步「添加范本」 + 规范弹窗高度）**：① **任务级范本**——在第二步「指定行文规范」的可选「添加范本」：`StyleGuideEditor` 增加 `taskId` 参数，工具栏「添加范本」置于「导入已有规范作为底稿」左侧并列；点击展开**可折叠范本窗口**（展开/收起逻辑参考第一步矛盾窗口：展开=文本框 + 底部固定「▲ 收起」，收起=有内容时显示「范本 ▼」条），录入防抖自动保存。**数据**：Migration 020 `writing_tasks.model_text TEXT NULL`；新 IPC `writing:getModelText`/`writing:setModelText` + preload + main handlers + `WritingTask.modelText`。**生成侧**：`generateDraft` 读取 `task.modelText`，非空时 `buildUserPrompt` 注入 `【参考范本】` 区块（与【写作规范】【参考材料】并列），并提示参考其体例与行文风格；不填范本时不出现该区块。② **规范弹窗高度**——头部「规范」弹窗 `.style-guide-modal` 由 `max-height` 改为固定 `height: 80vh`，文本编辑区随高度拉大（此前点击某篇规范后编辑区因高度自适应过低）。验证：typecheck 零错误、164 项单测、构建通过。
- **2026-08-25（Phase 6.4.1 规范文档库 + 第二步文本编辑器）**：按用户构思实现多篇规范文档库与第二步「指定行文规范」编辑器。**数据**：Migration 019 `style_guides` 表（多篇，`is_default` 全局唯一默认，启动时表空自动写入 `DEFAULT_STYLE_GUIDE`）；新仓储 `db/style-guides.ts`（list/get/save[有 id 覆盖、无 id 新建且首个自动设默认]/setDefault/delete[删除默认回退剩余第一篇]/ensure，含 4 项单测）。**IPC**：`styleGuide:list/save/setDefault/delete` + preload + main handlers。**UI**：`StyleGuideEditor`（textarea 展示当前默认规范；右上「导入已有规范作为底稿」→ 选择已保存规范 → 二次确认替换全文；右下「保存规范」→ 弹「选择保存方式」：已有规范列表 + 空白「+」，点已有项→「覆盖现有规范」，点「+」→「另存为新规范」输入名称；每篇可设为默认/重命名/删除），第二步内嵌显示；头部新增「规范」入口按钮（回收站按钮左侧并列）打开同款弹窗。**生成侧**：`buildSystemPrompt`/`buildUserPrompt` 改为读取 `getDefaultStyleGuide()?.content`（无则回退 `DEFAULT_STYLE_GUIDE`）注入。文档（shared-contracts/data-model/ui-architecture）同步。验证：typecheck 零错误、163 项单测、构建通过。
> 后续微调：头部「规范」按钮图标改为**三角板**，点击打开**规范列表**（点击某一篇进入编辑/保存）；第一步「矛盾」窗口增加**向上三角形**收起/展开按钮，且该按钮固定在矛盾窗口（可滚动列表）**下方、始终可见**，无需随列表滚动到底。后续修复：矛盾列表改为**容器整体上限 40% + 内部滚动区**（原把 40% 上限移到内层列表导致容器高度失控，矛盾多时把下方资料卡片挤出视口）。

- **2026-08-25（Phase 6.4 行文规范简化：删除 skills 模块 + 合并默认规范）**：① **删除整个「写作规范 skills」模块**——移除 `SkillsManager`/`SkillPickerDialog`、`writing-skills.ts` 仓储、`skills:*` IPC（list/create/update/delete）+ preload 方法 + `WritingSkill` 类型，删除「规范」页导航，移除 `resolveTaskSkills`/`listSectionSkills`/`matchSectionSkills`/`formatSkillsText` 及 `updateTaskSkillIds`；Migration 018 `DROP TABLE IF EXISTS writing_skills` 并 `UPDATE writing_tasks SET skill_ids = NULL`。② **合并仅保留的两篇通用规范（志书文体文风 + 志书行文规则）为默认规范** `DEFAULT_STYLE_GUIDE`（`src/shared/style-guide.ts`），作为默认规范注入第二步（行文规范）显示，并在生成初稿时作为全局写作约束注入 system/user prompt；`buildSystemPrompt`/`buildUserPrompt` 改为直接使用该默认规范。验证：typecheck 零错误、159 项单测、构建通过。

- **2026-08-25（任务自动改名 + 矛盾回收站）**：① 自动改名——`generateCompilation` 用大模型提取出标题后，若任务标题仍为默认“新建任务”，自动 `renameTask(taskId, extracted.title)`；用户仍可右键重命名。② 矛盾回收站——采纳/忽略某组矛盾时，除把未被采纳卡片“软删除”（`kept=0`，UI 只显示 kept=1）外，还把整组矛盾快照进新表 `compilation_recycle_bin`（Migration 017，引用 contradiction_id，随 compilation 级联删除）。右上角垃圾桶小圆钮进入回收站，可“恢复”某组矛盾：所有 variant 卡片改回 `kept=1`、矛盾状态回到 pending、删除回收站条目。**用软删除代替硬删除，恢复不重建卡片，避免重复卡片/卡片数异常**（单测验证恢复后卡片总数不变）。新增回收站 IPC（`compilation:recycleBin:list/restore`）+ preload + Repository + UI（垃圾桶圆钮 + 回收站弹窗）。验证：typecheck 零错误、160 项单测、构建通过。

- **2026-08-25（对话持久化 + 矛盾稳定性 + 卡片 UI）**：用户反馈三点并修复。① **对话历史持久化**——`compilation:generate`/`regenerate` 处理器现把撰写要求写入 `task.userInstruction` + `addTaskMessage('instruction')`；前端生成/重生成后持久化助理摘要并 `reloadMessages()`；`load()` 从最新汇编 `title` 恢复 `compilationInstruction`。因此关软件/切页后对话历史保留，“重新生成汇编”按钮能取到要求。② **矛盾稳定性**——逐窗细读会漏“两个相左说法在不同窗口”的跨窗口矛盾；新增 `scanCardContradictions` 在细读产出最终卡片后对精简卡片集做一次低成本 LLM 矛盾归类（`parseCardScanGroups` + `mergeContradictionGroups`，与窗口级矛盾合并去重），提升跨来源/跨窗口矛盾召回且不牺牲效率。③ **卡片 UI**——资料卡片每张独占一行；来源/编辑/删除收进右侧“…”下拉菜单（`menuFor` 状态）。新增 2 项单测（`parseCardScanGroups`、`mergeContradictionGroups`），共 160 项全通过；typecheck / 构建通过。

- **2026-08-25（Phase 6.1 三段式细节修正）**：用户逐条反馈三点，均已修改。① **段落划分**——粗筛改按原始换行划分（`retrieval.ts` 新增 `chunkParagraphs`，仅剔除标题行、不按句/字数二次切分），避免“最后一句话被截半”；AI 细读提示词改为“先判相关性、再按时间/事实/条目做更细切分、输出完整事实摘录（不截断）”，超长单段（>30000 字）在切窗时按句兜底拆分防上下文溢出（`sliceChunks`）。② **矛盾取舍**——采纳某卡后 `updateCompilationContradictionStatus` 自动删除该矛盾分组中未被采纳的卡片（级联清理 variant 行），前端重新拉取汇编同步删卡。③ **去除生成资料汇编阶段的写作规范**——移除 ChatPanel 输入框上方写作规范 UI（智能匹配/手动选择），删除 `WRITING_UPDATE_SKILLS`/`WRITING_SUGGEST_SKILLS` 通道与 handler、preload `updateTaskSkills`/`suggestSkills`、`generate.ts` 的 `suggestSkillsForTask`/`parseSuggestSkillsOutput`；`writing_skills` 数据管理（规范页）与初稿生成侧自动注入保留。新增 2 项单测（`chunkParagraphs`、采纳删卡），共 158 项全通过；typecheck / 构建通过。

- **2026-08-25（Phase 6.1 大模型提取标题与粗筛关键词）**：用户指出静态 `extractTopicTerms` 只取引号内核心词，覆盖不了“包含例如：…托儿所/招生/等级/占比”等细节词，粗筛可能漏掉只含这些词、不出现“学前/幼儿园”的段（窗口偏少的主因之一）。按用户提议实现：调用大模型前**先用大模型**（配置了 Provider 时）从用户完整撰写要求中提取标题 + 近义词/上下位词/专业词（理解方志语境做扩展），返回 `{title,keywords}`；据此生成 `coarseQuery`（词法粗筛词）与 `vecQuery`（标题向量）。新增可测的 `parseKeywordExtraction`（解析 JSON）与本地兜底 `fallbackCoarseQuery`（extractTopicTerms + expandDomainHints）；大模型调用失败或无 Provider 时自动回退本地。粗筛阶段因此能保留“托儿所/招生/等级/占比”相关的段，再由模型细筛判断。新增 3 项单测（156 项全通过）；typecheck / 构建通过。

- **2026-08-25（Phase 6.1 汇编相关性修正，最终形态）**：用户实测发现资料汇编混入“福州市先进教育工作者/优秀教师”等荣誉卡片（来源《长乐志》，263 卡、候选 2660 段）。根因：① AI 细读提示词未注入用户撰写要求，模型按“志书汇编”泛化标准筛选；② 词法闸门把“教育”双字对命中即算相关，荣誉记录混入候选。**最终修正（按用户要求回调后）**：a) 把 `instruction`（撰写主题与范围）注入 system/user prompt，模型按用户标题自行判断相关事实并提炼卡片，**不再显式列举“排除荣誉/党建/后勤”等类别**；b) 词法闸门**回退为宽松粗筛**——仅剔除无任何信号（score==0 且无向量命中）的“肯定无关”段，有任意词法或向量（≥0.1）信号的段都保留，交由模型细筛，既不因“教育”单字对误判而候选过多，也不因收紧而误删“公办园数量”这类相关但无字面重叠的段；c) 卡片移除“位置：第 N 段”注释。验证：typecheck 零错误、153 项单测、构建通过。

- **2026-08-25（Phase 6.1 汇编候选性能优化：保守本地闸门）**：用户实测“学前教育园所设置”生成汇编时 AI 细读出现 94 个窗口、预计 31 分钟，过长且费钱。优化——在 `generateCompilation` 调用大模型前新增 `recallCompilationCandidates` 保守闸门，把提交物从“任务范围内全部段落”收敛为“与主题相关的来源及其相关段落”：①来源级：仅保留有相关信号的来源（标题含查询词 / 任一段词法 score>0 / 任一段向量余弦 ≥0.1），完全无关的来源整篇舍弃（资料库中大量无关文件是窗口数主因）；②来源内：标题含任一查询词或来源较小且词法信号强 → 整篇保留（篇内不漏）；宽口径来源（如综合年鉴）只保留有信号的分块。向量路径用低阈值（0.1）兜底“字面无关但语义相关”段落（如含地点名的数据段）。无 Provider / AI 失败时回退用全量集合（不因闸门丢材料）。为省去无意义开销，把 `embedTexts` 移到无 Provider 检查之后。新增 4 项单测（153 项全通过）；typecheck / 构建通过。

- **2026-08-25（Phase 6.2/6.3 三步向导前端 + 生成链路改造）**：`WritingWorkspace.tsx` 重写为三段式向导——顶部 `writing-stepper` 步骤条（未确认汇编时锁定 Step 2/3，仅可回退）、右栏随步骤切换（Step 1 `CompilationStep` 卡片审阅 / Step 2 行文规范占位 / Step 3 初稿编辑器）、左侧 `ChatPanel` 贯穿（Step 1 主按钮「生成资料汇编」走 `onPrimaryAction`；Step 2 预留自由对话；Step 3 生成初稿），`compilation:progress` 事件驱动候选统计与进度条；`CompilationStep` 空态不再放误导性按钮（改为引导左侧输入），卡片操作/矛盾取舍回写本地状态。`generate.ts` 的 `generateDraft`/`regenerateDraft` 新增可选 `compilationId` 分支——已确认汇编时仅取 `kept` 卡片文本（按时间排序）作为材料，跳过摘要/网页/检索/矛盾扫描，未提供时保留旧检索链路；修复 `regenerateDraft` 未透传 `compilationId` 的问题。新增 CSS（`.writing-stepper`/`.writing-style-step`/`.compilation-*`）。验证：typecheck 零错误、149 项单测、生产构建成功。待用户实测三步端到端与基于汇编生成初稿。

- **2026-08-25（Phase 6.1 资料汇编生成服务）**：新增 `writing/compilation-service.ts`——①本地宽召回 `recallCandidateChunks` 直接返回任务范围内全部有效分块（词法分仅用于排序，**不做任何过滤**，从机制上保证不丢相关材料）；②AI 分窗细读（≤30000 字/窗、并发 2、温度 0→0.3 重试、maxRetries 1），提示词要求逐字摘录/提取时间标签/来源编号/发现实质冲突，输出 `{items,contradictions}`；③解析/合并去重/#N 映射/按年份排序落库；④无 Provider 或 AI 未产出有效卡片时降级为「全部候选块直接成卡片（ts 取首个年份）」。`compilation:generate`/`regenerate` handler 接入服务并推送 `compilation:progress`（候选块数/来源数/窗口进度）。新增 5 项单测（宁多勿漏召回、JSON 解析、#N 映射、合并去重、空输入）。真实 Provider 的 AI 细读/矛盾标注效果待用户实测。

- **2026-08-25（Phase 6.0 资料汇编数据模型）**：三段式撰写重构启动——Migration 016 新增 `compilations`/`compilation_items`/`compilation_contradictions`/`compilation_contradiction_variants`；`shared/types.ts` 新增 Compilation/Item/Contradiction/Variant；`shared/ipc.ts` 新增 `compilation:list/get/generate/regenerate/updateItem/deleteItem/resolveContradiction/confirm`；新仓储 `db/compilations.ts`（创建/读取、卡片写入/编辑/删除、矛盾写入/取舍[resolve 校验卡片归属]、确认 finalize、级联删除）+ 4 项单测；主进程 CRUD/取舍/确认 handler 已实现，`generate`/`regenerate` 契约登记、AI 服务待 Phase 6.1。已确认决策固化在 `PLAN.md` Phase 6：本地宽召回+AI 细读（**宁多勿漏**）、深改 TipTap、三主题发布版可切换、三步为独立页面由向导切换、矛盾必须取舍后才能进下一步、通用规范默认注入、初稿仅汇编层溯源。验证：typecheck 零错误、144 项单测、生产构建成功。

- **2026-08-24（移除手动「同步工作区」）**：决策——手动同步按钮及其调用的 `workspace:reconcile` 通道、preload API、i18n 文案与 SourceList 相关逻辑整体删除；同步改为纯自动（启动 / 窗口聚焦 / 进入资料库 / 每分钟定时 / 设置页变更工作区 / chokidar 监听增量）。理由：自动触发已全覆盖手动按钮的功效，保留只会多一个入口与 UI 负担，删除后涉及「手动同步」的代码面归零，也消除了未来新增调用路径时绕过互斥调度的风险。清理范围：`shared/ipc.ts`（WORKSPACE_RECONCILE 通道与 WorkspaceReconcileRes 类型、IpcMapping 项）、`main/index.ts`（handler）、`preload`（方法 + 类型声明）、`SourceList.tsx`（按钮/说明气泡/handleReconcile/reconciling/manualReconcilingRef）、`zh-CN.ts`（reconcileBtn/reconciling/infoReconcile/reconcileFailed）、`docs/shared-contracts.md`、`docs/ui-architecture.md`。自动同步完成事件保留列表刷新与提示逻辑。验证：typecheck 零错误、140 项单测、生产构建成功。

- **2026-08-24（重复入库二次审计）**：重新盘点全部触发工作区对账/扫描的机制——启动、窗口聚焦、进入资料库、每分钟定时、手动同步、设置页变更工作区、chokidar 监听增量、存量迁移。结论：v0.1.3 的互斥调度 + 入库前复核 + Migration 015 唯一索引已覆盖并发重复入库主路径；本次再加固三处残留缝隙：① 手动同步在排队期间被合并后不再直接调用 reconcileWorkspace（避免微小并发窗口），改为循环重提调度器直至拿到结果；② workspace:migrate（迁移存量资料）纳入调度器串行，避免迁移搬入的文件被并发对账先入库后 UPDATE 撞唯一索引；③ 工作区新扫描到的文件若与「旧版导入副本（workspace=0）」内容哈希一致，改为吸收旧记录（保留 id/标签/摘要并升级为工作区记录），消除「旧副本 + 工作区记录」并存的另一类重复；设置页切换工作区后监听重启保留进度回调。新增 2 项单测（调度器 await 契约、旧副本吸收）。验证：typecheck 零错误、140 项单测、生产构建成功。

- **2026-08-20（工作区重复入库修复）**：资料列表每个文件显示两次的根因是**并发对账**——设置页变更工作区与手动「同步工作区」直接调用 `reconcileWorkspace`，绕过 `auto-sync.runWorkspaceSync` 互斥；窗口聚焦/进资料库/每分钟定时/监听增量均经调度器。多个全量/增量对账同时扫描到同一批新文件，各自在「查不到库记录」后依次插库 → 同一 `file_path` 产生多行。修复：① `runWorkspaceSync` 升级为可 await 的调度器（busy 期间只保留最新任务排队，队列清空后 resolve 等待者），设置变更与手动按钮全部改为经该调度器；② `ingestFile` 在解析后插入前复核路径（并发兜底，已存在则更新指纹而非重复插入）；③ Migration 015 清理存量重复行（每个 file_path 保留最早一条，级联清理其关联）并建立部分唯一索引（workspace=1 文件按 file_path 唯一），结构性杜绝复现；新增迁移 015 回归单测。验证：typecheck 零错误、138 项单测、生产构建成功。另注：若重复还来自「旧版导入副本（workspace=0）+ 工作区记录并存」，属另一类数据，可经设置页「迁移旧资料到工作区」收敛。

- **2026-08-20（右键粘贴修复）**：Electron 沙箱渲染进程无法直接读系统剪贴板，导致文本框中 Ctrl+V 正常、右键无粘贴。新增 `clipboard:readText`/`clipboard:writeText` 两个 IPC 通道（主进程 `clipboard` 模块读写，preload 暴露 `readClipboardText`/`writeClipboardText`）；新建全局 `TextContextMenu`（input/textarea/contenteditable 右键菜单：剪切/复制/粘贴/全选，粘贴经主进程读剪贴板，React 受控输入用原生 setter + input 事件同步）；`DraftEditor` 正文右键菜单同步扩展为「剪切/复制/粘贴/全选 + 询问文段来源」，未选中文本时按右键位置定位光标。验证：typecheck 零错误、137 项单测、生产构建成功。

- **2026-08-19（本轮大规模优化）**：① **文档整理**——`agents.md`/`PLAN.md` 将历次已完成任务的冗杂过程合并为「已完成阶段摘要」，保留方案结论与根因；`docs/shared-contracts.md`/`data-model.md`/`ui-architecture.md` 同步到代码现状（skills 替代范本、网页资料库、矛盾表、task_id 缓存文章、诊断日志通道等）。② **接口清理**——`shared/ipc.ts` 删除无实现无调用的死通道（`sources:refresh`、`segment:resolveConflict/addManual/insertGenerated`），新增 `app:openFileDialog`/`app:openDirectoryDialog` 常量与 `workspace:progress`/`writing:streamDelta` 事件常量（preload/main 改引用常量），修正 `sources:get` 响应类型为 `{ source, tags }`。③ **隐患与性能修复**——`file-parser.parseImage` 失败路径不再泄漏 tesseract worker；`url-fetcher` 空响应体防御；`drafts.splitMarkdownIntoSegments` 标题正则容忍 `##标题`（无空格）；`retrieval` 新增按 `sourceId+contentHash` 的分块缓存 + 查询 bigram/词条预计算（大资料重复检索显著提速，无 contentHash 的存量资料不缓存避免碰撞）；`generateDraft` 幂等检查前置到所有副作用之前（避免重复写消息/整理摘要/抓取网页/检索扫描）。④ **LLM 交互稳定性与速度（用户选定方案 A+B）**——`chat.ts` 新增瞬时故障自动重试（HTTP 429/5xx/网络错误，指数退避 0.8s/2s/4s，经 `ChatCallOptions.maxRetries` 开启；生成/对话 2 次、摘要/来源询问/智能匹配 1 次；401/超时/空响应/格式错误不重试）+ **SSE 流式输出**（`onDelta` 回调 + `createJsonFieldStreamer` 只把生成输出 JSON 中的 content 字段增量推给前端，原始 JSON 不刷屏；非 SSE 响应回退普通 JSON 解析）；新增 `writing:streamDelta` IPC 事件，`ChatPanel` 实时流式气泡 + 光标动画，生成/对话完成后再以持久化消息为准刷新。⑤ **前端视觉与交互打磨**——补齐历史遗留未定义 CSS 变量（`--bg/--bg-soft/--bg-elevated/--fg-faint` 等）；统一按钮过渡/焦点环/滚动条/模态动效；顶栏品牌标识；左栏选中指示条；资料列表选中高亮（App 传 activeId）；任务列表第二行显示文章标题/创建时间；编辑器页脚字数统计；对话面板智能自动滚动（阅读历史时不抢滚动）、打字动画、流式气泡、空状态引导步骤；ConfirmDialog 支持 Esc 取消；各处加载态统一 spinner；SourceList 硬编码文案迁入 zh-CN（`sourceStatus.*` 等）。**设置页重设计**——中栏由空占位改为「设置导航」（总览/工作区/预设/Provider 四项，点击平滑滚动 + IntersectionObserver scroll-spy 高亮）；右栏改卡片式区块（分区图标 chips、总览渐变卡含当前 Provider/工作区状态速览与新手教程/导出日志入口、工作区状态 chip、预设卡片头像与悬停、Provider 条目头像/模型 pill/密钥状态圆点）。**规范页同风格重设计**——导航组件抽成通用 `SectionNav`（设置/规范两页共用）；规范页中栏改「规范导航」（总览/通用规范/部类细则），右栏改总览渐变卡（条数统计 + 新建入口）+ 搜索工具栏（清空按钮）+ 通用规范/部类细则两张卡片式区块（图标 + 说明 + 数量、条目头像/徽标/关键词 chips/内容预览），新建/编辑/删除逻辑不变。**验证**：typecheck 零错误、137 项单测、生产构建通过。
- **2026-08-14**：生成链路解耦重构——矛盾扫描/网页检索改用"标题词 + 领域下位词"稳定主题查询（`extractTopicTerms`/`expandDomainHints`），与生成正文 chunks 解耦，避免同一主题不同措辞导致两次生成材料不一致；`clusterSourcesByTopics` minDice 0.05→0.12（泛教育政治学习文章不再聚成超大簇）；网页召回下位词表再收窄（剔除"招生/校历/学位"等）。新增智能匹配写作规范（`writing:suggestSkills`，temperature 0）；诊断日志系统（IPC 调用/LLM 调用/提交物记录 + 一键导出，敏感信息脱敏）；新手引导聚光教程（设置页可重开）；撰写工作台常驻挂载（切页不丢对话/进度）。
- **2026-08-13**：写作规范 skills 重构（见设计决策）；网页缓存文章绑定任务（Migration 013，资料库只显示 `task_id IS NULL` 的长期资料）；矛盾扫描要求"不同来源相左"（同一来源总分关系不算矛盾）；`matchesExact` 正文精过滤替代 bigram 模糊。
- **2026-08-11**：Phase 3.7 增强（采纳本地修订 + 矛盾/警告分类 + 撤销兼容 + 生成进度条）；矛盾扫描提速（整组窗口 + 并发 2 + 空结果重试收敛）与视野收敛（只扫检索文段）；删除版本管理模块（代码移除、数据库保留旧列）。
- **2026-08-10**：Phase 3.7 三次调用链路 + 矛盾数据模型（Migration 009）+ 编辑器标注 + 弹窗 + 来源打开。
- **2026-08-09**：工作区加回大文件卡顿根治——索引改后台串行队列 + WASM 推理移入 worker_threads（embed.worker.js，崩溃/缺失自动回退主进程推理）；"伪实时同步"修复（对账完成事件带计数，SourceList 自动刷新）；UI 三连（图标导航/撰写左右栏/中栏显隐持久化）。
- **2026-08-08**：Phase 3.5 三连修（对话历史持久化 Migration 008 + llm_call_logs 痕迹；生成进度事件；对话超时 5 分钟）；"学前教育"生成耗时 5 分 44 秒排查结论：材料量大所致（用户决策仅加进度提示）；聊天式工作台；一键复制；.wps/.xls/.xlsx/.doc 格式支持（按文件头签名分发）。
- **2026-08-07**：Task 3.4 系列——连续整稿编辑器（`draft:updateContent` 整稿保存重建片段）；摘要粗筛（无摘要资料保守保留）；整篇连贯正文（去除 JSON 片段契约）；标题行剔除修复"初稿只有几行字"；重新生成初稿；范本剔除目录页；取消材料供给限制（过滤式检索）；生成超时 10 分钟；生成前自动整理范围内摘要。
- **2026-08-06**：工作区资料库全面重构 + 批量录入异步化（fs/promises + 分批让出事件循环，300 文件全量对账 0.6s）；BGE-small-zh-v1.5 模型落地 + WASM 后端固化（本地 `ort.env.wasm` 路径配置）；窗口/布局状态持久化；输入框失焦 bug（原生 confirm 导致，全部替换为自定义 ConfirmDialog + `window:focus` 恢复激活）。
- **2026-08-03~05**：Phase 1/2/3.1 完成；标签系统重构（独立关联、去颜色、去标题嵌入）；TipTap 编辑器选型；RAG 词法 bigram 检索。

## 已知问题

- **大模型幻觉风险**：无依据编造史实是必须重点抑制的风险，通过"信源白名单 + RAG 检索 + 逐片段来源标注 + 提示词硬约束（严禁编造）"在架构层面约束。
- **生成耗时与稳定性**：材料量大时单次生成可达数分钟（10 分钟超时）；矛盾扫描/定位的温度阶梯重试带来额外 LLM 调用。2026-08-19 已落地瞬时故障自动重试 + 流式输出（用户选定）；未采纳的备选方案（生成结果缓存、扫描并发提速、材料瘦身）记录于本轮优化记录，可按需再议。
- **向量索引后台化**：索引为后台异步，生成初稿前如索引未完成会自动降级为纯词法检索（既有兜底）；大 PDF 解析（pdf-parse 同步 CPU）仍短暂占用主进程。
- **FTS5 中文全词检索受限**：unicode61 对中文按单字分词；资料列表关键词过滤英文正常、中文受限（RAG 检索走 bigram 打分不受影响）。
- **网页资料库实站效果**：站点抓取（网络/反爬/栏目结构）依赖真实环境，需用户注册站点后实测。
- **better-sqlite3 原生模块分发**：首次 `npm install` 需要编译工具（node-gyp/VS Build Tools）；electron-builder 打包配置 `nativeRebuilder` 保证跨机器可运行。
